# Driver Incentives v1 — Driver App — Prototype Tracker

> **IMPORTANT:** This tracker drives the v0 build queue. **v1 is a STRIP-BASED build** — start from a duplicate of the v3-baseline v0 chat and prompt v0 to REMOVE everything that belongs to v2 or v3, then migrate the schema. NOT a from-scratch rebuild.
>
> Re-read this file + `PROTOTYPE-BIBLE.md` if context compacts.

## Build Approach

**Duplicate the v3-baseline v0.dev chat** (use v0's "Duplicate" / fork-branch action on the latest v3 chat — creates a new chat that starts from the same code state). Work on the duplicate. The original v3 chat stays frozen at the v3 baseline.

**Each strip step is forward-only on the duplicate.** Do NOT revert. Do NOT recreate stripped components later. If a feature is removed in I-0, it's gone for v1 — it lives in the original v3 chat + `v3/References/` docs only.

**Re-orientation prompt (Prompt 0)**: re-uploads the v1 BIBLE + this v1 TRACKER (overwriting the v3 versions in the duplicated repo) and confirms v0 understands the v1 scope before stripping begins.

**Why 3 steps instead of 5:** The existing UI is already CEO-approved; we're scoping down, not rebuilding. I-0 is one big strip pass (Variant Toggle + Tier + Leaderboard + `/payout` page + UpcomingPayoutWidget removed entirely + Earned popup downgrade). I-1 is the only ADD step (per-incentive admin schema). I-2 is polish + grep sweep. Minimizes prompt rounds while keeping each prompt focused on a single intent.

## Current Step
🟢 **App side QUIESCENT post-2026-05-14 — small drift patches in 2026-05-15 single-session waves (recolor + pause filter).** App lockstep with Manager pause/recolor/canonical-purple work: (1) all 8 App seed incentives recolored to canonical purple `#8B5CF6` via batch perl (matches Manager `INCENTIVE_PILL_COLOR` export); (2) `getAllIncentiveProgress` filters out `enabled === false` so drivers don't see paused incentives. All other 2026-05-15 waves were Manager-only (POLISH/POLISH2/ORDER 1-7/PAUSE kebab UI/FILTER 1-2/RESUME 1-3/FILTER-UNIFY/GUARD 1-2 + bug fixes — none of which touched App-side UX). App MVP queue remains LOCKED at 10/10 ✅. **🔜 Next phase: PRD / story drafting (Phase E entry).** App is driver-facing surface; PRD scaffolding via `prd-finalizer` will produce `PRD - v1 - Driver App.md` capturing post-MVP-2 stripped state (header card + Completed rides only; plain outline button; no Appeals subsystem; pause-filter on dashboard).

### Earlier same day — pre-drift-patches context preserved
🟢 **App-MVP-2 ✅ landed 2026-05-14 LOCAL ONLY — Appeals Surgical Strip (App).** App side of MVP wave COMPLETE — no further App prompts in v1 MVP queue. Lockstep returns to Manager-only (MVP-3 next). **Phase A — App-side strip (6 deletes + 7 edits):** DELETED canonical Appeals data layer (`lib/data/appeal-acks.ts` · `lib/data/disqualifications.ts` · `lib/data/disqualification-helpers.ts`) + 3 driver components (`criterion-card.tsx` · `dispute-appeal-sheet.tsx` · `incentive-status-pill.tsx`). EDITED `app/page.tsx` (dropped `AppealReentryPopupHost` mount); `incentive-rides-view.tsx` (major rewrite — inline `seedTrips` filter; dropped Eligibility section, CriterionCard list, DisqualifiedBanner, DisputeAppealSheet state machine, all appeal state derivations; view = header card + Completed rides only); `dashboard-incentive-section.tsx` (dropped `useDisqualificationsState`/`getIncentiveStatus`/`IncentiveStatusPill`/status-tone styling/icons; button = plain outline); `app/incentives/page.tsx` (derived status from `progress.isComplete` only); `past-outcomes.ts` (dropped `"missed-criterion"` union member + appeal-denied seed); `incentives.ts` (dropped `driverTargeting?` field + all seed array literals); `incentive-utils.ts` NO-EDIT. **Phase B — Manager preview re-vendor** (Manager working dir): DELETED 6 vendored Appeals files + helpers; re-vendored 5 components + 4 helpers from post-strip App canonical with `Source last synced: 2026-05-14`; Manager preview view consumers needed no edits. **Verification (acceptance gate met):** grep both prototypes case-insensitive `appeal|disqualif` → zero active-code Appeals hits (remaining: MVP-1 strip docblocks + `match-drivers.ts:20` P-12 hook comment + unrelated `lib/agent-mock/drivers-data.ts` onboarding "disqualifying" string — driver onboarding, not Appeals). App `/incentives/[id]/rides` = header card + Completed rides only; App dashboard button = plain outline; Manager `/incentives/[id]` renders 3 tabs only; embedded driver-app preview pane post-strip (no "Action needed", no IncentiveStatusPill, no Appeals variants). Schema untouched (`:v3` stays). Both dev servers HTTP 200. Path C local-first; v0 backfill pending. **🔜 Next paste = Manager MVP-3 (More Filters drawer respects column visibility; Manager-only; Auto model) on Manager working dir `v1/Incentives V1 CS Tool/` at `:3001`.**

### Earlier same day — pre-App-MVP-2 context preserved
🟢 **App-I-7 + App-I-7.1 polish ✅ LANDED 2026-05-13 LOCAL ONLY.** App-I-7.1 (same-day inline polish, folded into App-I-7 approval) closed three connected discoverability + consistency gaps surfaced during preview review: (a) **promoted Your Rides button** picks up the status pill's tone + icon when action-class status (`temp-disqualified` amber `AlertTriangle`, `appeal-under-review` sky-blue `Clock`, `appeal-lost` red `XCircle`) so the connection between pill and action path is visually obvious; (b) `CriterionCard` muted pill **"Appeal pending" → "Appeal submitted"** + new 5th `IncentiveStatus` `'appeal-under-review'` split out of `temp-disqualified` (pending-only criteria no longer surface "Action needed"); (c) **unified `<DisputeAppealSheet>` to 4 modes** (`create` / `view-pending` / `view-approved` / `view-denied`) + **deleted standalone `<AppealResultDialog>`** so all four appeal interactions share the same Sheet shell + criterion banner + "Your appeal" section (Manager response section differentiates won/lost). `<AppealReentryPopupHost>` moved into the Sheet file. 8 files changed (1 deleted, 7 edited).

 Wires the real `<DisputeAppealSheet>` (mode-aware: `create` / `view-pending`) behind App-I-6.1's placeholder alerts on the ✗ `<CriterionCard>` Appeal / Appeal-pending buttons, refines `<AppealResultDialog>` for criterion-level copy + amber-warning tone on denied, and adds a one-shot dashboard re-entry pop-up that surfaces unacknowledged resolved appeals FIFO.

**6 files changed (3 new + 3 edits):**
1. **NEW** `lib/data/appeal-acks.ts` — `getAcknowledgedAppealIds()` + `acknowledgeAppeal(appealId)` localStorage helper. Append-only `string[]` persisted at `wingz-incentives:appeal-acks:v1` (reserved key declared during App-I-6). Defensive parse (non-string entries filtered). Idempotent — already-acked IDs are a no-op so safe to call on every dialog open.
2. **NEW** `components/driver/dispute-appeal-sheet.tsx` — `<DisputeAppealSheet>` shadcn `Sheet` bottom-side mode-aware component. Header: incentive title + "Appeal a missed criterion" (create) / "Appeal pending review" (view-pending). Red-tinted pre-filled banner reads `formatDisqualificationReason(disqualification)` from App-I-6.1 helpers — criterion-level (NOT trip-level) per design pivot. **Create mode body:** `<Textarea>` reason (required, MIN_REASON_LENGTH=10 trim-then-length validation, state-aware helper copy "A reason is required (min 10 characters)." / "Add at least N more characters." / "Min 10 characters; this will be shown to the manager."; aria-invalid on touched-empty; destructive border when in-error); optional comments `<Textarea>`; sticky Cancel ghost + Submit Appeal emerald primary footer (Submit disabled while `reasonTooShort`); on submit calls parent-provided `onSubmit(driverText)` where `driverText = reason + "\n\n" + comments` (or just reason). **View-pending mode body:** read-only `Appeal.driverText` display in gray card + "Submitted MMM D · pending manager review" line; single Close outline footer button. Local form state resets via `useEffect` on open=false. **Refactored mid-prompt:** parent owns `useDisqualificationsState()` hook + passes `createAppeal` indirectly via `onSubmit` callback — previously the sheet held its own hook instance which writes-but-doesn't-share-state with the rides view, so the criterion state machine wouldn't see the new appeal until reload. Lifting state to the parent fixed the flip without needing a context provider.
3. **EDIT** `components/driver/appeal-result-dialog.tsx` — refined per spec: denied variant tone switched red → **amber** (`bg-amber-100` icon ring + `AlertTriangle` icon + `text-amber-800` title + amber bordered manager-reason section); approved variant stays emerald + `CheckCircle2`. Copy reframed to criterion-level via new `incentiveTitle` prop: approved → "{title} — the {criterionLabel} requirement is exempted for this campaign."; denied → "{title} — the {criterionLabel} disqualification stands.". Headlines flipped to "Your appeal was approved" (approved) / "Appeal denied" (denied). `handleDismiss` calls `acknowledgeAppeal(appeal.id)` before closing — idempotent. **NEW exported `<AppealReentryPopupHost>` component** — scans `appeals` for the current driver where `status !== 'pending'` AND `id NOT IN acks`, sorts FIFO by `resolvedAt ?? createdAt` ascending, picks the head, renders the dialog; on dismiss bumps a `scanTick` to re-scan for the next candidate. Looks up the linked `Disqualification.failedRule.type` + matches it to the incentive's `driverTargeting` array to compute the exact criterion label (defensive fallback to first rule if no match).
4. **EDIT** `components/driver/incentive-rides-view.tsx` — replaced placeholder alerts in `handleOpenAppealSheet` with real `setAppealSheet({...})` state updates (preserved 4-state machine: create | view-pending → sheet; won | lost → result dialog). Added `<DisputeAppealSheet>` mount with `onSubmit` plumbed to the local hook's `createAppeal({ driverId: CURRENT_DRIVER_ID, incentiveId: incentive.id, disqualificationId, driverText })`. Added `incentiveTitle={incentive.title}` to the existing `<AppealResultDialog>` mount. Added `acknowledgeAppeal(state.appeal.id)` in `handleOpenAppealResult` so paper-trail opens via "Appeal won/lost" buttons also satisfy the ack gate (idempotent — already-acked IDs are no-ops).
5. **EDIT** `app/page.tsx` (dashboard) — mounted `<AppealReentryPopupHost />` after `<BottomNav />`. Host is null-rendering until a candidate exists so it's safe to mount unconditionally.

**Verified in preview at `http://localhost:3000` 2026-05-13:**
- Peak Performer (`/incentives/inc-pp-001/rides`) → OTP criterion already has pending appeal (ap-001), shows "Appeal pending" pill → tap opens view-pending sheet with banner "OTP dropped to 92% — required 95%." + submitted text + "Submitted May 10 · pending manager review" + Close button. ✅
- Weekend Warrior (`/incentives/inc-ww-001/rides`) → OTP criterion has no appeal (dq-004 no-appeal), shows "Appeal" primary CTA → tap opens create sheet with banner "OTP dropped to 87% — required 90%." + Textarea reason validation (`< 10 chars` → helper + Submit disabled; `≥ 10 chars` → helper softens + Submit enabled). Submit with valid reason → toast "Appeal submitted — Your appeal is pending review for Weekend Warrior." + sheet closes + criterion card button flips **immediately** "Appeal" → "Appeal pending" (no reload needed). Re-tap "Appeal pending" → view-pending sheet shows the just-submitted text + "Submitted May 13 · pending manager review". ✅
- Dashboard re-entry pop-up: cleared `wingz-incentives:appeal-acks:v1` → mounted `/` → first popup fires FIFO with ap-past-001 (Peak Performer — Late April, denied, amber + "OTP ≥ 95% over 30 days disqualification stands."). Dismiss → ack written → next pop-up fires (ap-004 Quick Wins denied Sendbacks). Dismiss → ap-002 White Glove approved emerald + "OTP ≥ 98% over 30 days requirement is exempted for this campaign." (note: criterion exempted copy uses incentive's first matching rule). Dismiss → ap-003 Hometown Hero denied Sendbacks. Dismiss → no more popups. Reload `/` → no popup (acks persisted). ✅
- TypeScript clean; no console errors; only fast-refresh warnings during dev iteration (expected).

**No schema change** — App-I-7 only WRITES appeals (`createAppeal` already exposed via `useDisqualificationsState` since App-I-6) + READS appeals.status + writes acks to localStorage. `wingz-incentives:appeal-acks:v1` was reserved during App-I-6; this prompt activated it.

**🔜 NEXT PASTE (cross-prototype) = P-14 NEW (Manager Incentive Details Tab Restructure + Read-Only-by-Default Edit Gating; Auto model; Manager-only — no App pair; ships AFTER App-I-7 BEFORE I-7.5)** on Manager working dir `v1/Incentives V1 CS Tool/` at `:3001`. **P-14 NEW queued** (Manager Incentive Details Tab Restructure + Read-Only-by-Default Edit Gating; Manager-only — no App pair; ships AFTER App-I-7 BEFORE I-7.5). Earlier breadcrumb: Manager **P-13a ✅ landed 2026-05-12 earlier** (Appeals Review Skeleton — read path only; built `<AppealsSection>` as third stacked section with N pending amber badge + Pending/All segmented toggle + `<AppealDetailPane>` shadcn Dialog with 3 body sections; same-session follow-up added 4 appeal-count columns to Incentives list at `/incentives` with Pending visible by default + yellow row highlight; Total/Won/Lost hidden by default toggleable; 6 files Manager-side). **App-I-6.1 + inline scope extension App-I-6.1.1 ✅ landed 2026-05-12 LOCAL ONLY.** App-I-6.1 dropped the `Counted ⏐ Missed Out` tab strip; renamed route `/history` → `/rides`; added always-on Eligibility criteria block + 4-state appeal button per ✗ card; created `<CriterionCard>` + `<DisqualifiedBanner>` + `<AppealResultDialog>`; extended `IncentiveDefinition` with `driverTargeting?: DriverTargetingRule[]` (documented subset of Manager P-11 schema). App-I-6.1.1 added list-view status pill via new `<IncentiveStatusPill>` + `getIncentiveStatus` helper; refreshed seed (dropped dq-008 + bumped White Glove progress 3 → 6/6) to give clean per-pill-state demos: 1 In progress (Early Bird) + 4 Temp disqualified (Weekend Warrior no-appeal, Peak Performer pending, Squad Goals pending, Loyalty Streak no-appeal goal-hit) + 2 Appeal lost (Hometown Hero, Quick Wins) + 1 Earned (White Glove). After App-I-6.2 ✅ → cross-prototype lockstep resumes: Manager P-13a ✅ → P-13b → App-I-7 → P-14 NEW → I-7.5 → I-7.

— Historical context (pre-ideation, pre-App-I-6.1): User reviewed App-I-6 ✅ landed 2026-05-12 LOCAL ONLY and flagged 7 framing issues.

— Original App-I-6.1 spec context (now landed): NEXT PASTE = App-I-6.1 (Per-Incentive Rides View + Always-On Criteria List + 4-State Appeal Button) — Higher-thinking. Ships on App working dir `v1/Incentives V1 App/` at :3001. Post-compact ideation on App-I-6 framing **CLOSED 2026-05-12** with two follow-up prompts queued: App-I-6.1 (per-incentive view reframe) + App-I-6.2 (driver-level past archive on `/incentives` page). Read **Working Plan §6 Resolutions** for the locked design choices + the 7 original issues + their resolutions. **App-I-6 code stays at commit `10a8692`**; App-I-6.1 refactors the view-component on top. **Locked design:** drop tabs entirely; always-on Eligibility criteria block with one ✓/✗ card per Driver Targeting rule; 4-state appeal button per ✗ card (`Appeal` / `Pending` slide-up sheet / `Won` green button → dialog / `Lost` red button → dialog); approved appeal flips ✗ → ✓ exempted with "Won" button as paper trail; rides list unconditional below criteria block; route `/incentives/[id]/history` → `/incentives/[id]/rides`; CTA `"View history →"` → `"View rides →"`. **App-I-7 spec corrections folded in** (see Step App-I-7 spec below): `<DisputeAppealSheet>` mode-aware (`create` / `view-pending`); pre-filled banner reads from criterion not trip; `<AppealResultDialog>` shared with App-I-6.1 won/lost button interactions. After App-I-6.1 ✅ → paste App-I-6.2 (Past segment on `/incentives` page; driver-level archive; outcome badges Earned/Missed-goal/Missed-appeal-lost; tap row → `/rides` frozen end state; seed `seedPastOutcomes` table; Higher-thinking). After App-I-6.2 ✅ → cross-prototype lockstep resumes (Manager P-13a → P-13b → App-I-7 → I-7.5 → I-7).

— Historical context (pre-ideation): User reviewed App-I-6 ✅ landed 2026-05-12 LOCAL ONLY and flagged 7 framing issues — view-component infrastructure works mechanically (commit `10a8692`) but the conceptual model needs redesign before App-I-7 ships. **Read Working Plan §6 "POST-COMPACT IDEATION QUEUE" first.** The 7 issues: (1) "View history" CTA → "View rides"; (2) Counted ⏐ Missed Out framing wrong — disqualification is criteria-failure STATE, not per-trip EVENT; (3) goal-period-expiry isn't a trip-event; (4) rolling-window-not-met isn't a trip-event; (5) OTP-dip is recoverable rolling state ("if OTP gets back up trips finished still count toward the goal"); (6) eligibility model based on Driver Targeting criteria with 2 recovery paths (work-toward-eligibility / appeal-specific-event for sendbacks); (7) CRITICAL — goal progress continues during ineligibility (driver at 5/8 can reach 8/8 while ineligible; if eligibility restored or appeal approved by campaign end → earned). Phase B sign-off items marked Superseded: "W2-2 + W3-A1 + W3-A2 merge into Counted ⏐ Missed Out" + "Disqualification eval = LIVE per-trip" (partial — sendbacks still per-event; OTP/DSLA/goal-window are rolling-state). Ideation outputs needed: (a) "View rides" CTA copy fix; (b) view-component redesign (likely Rides + Eligibility/Status); (c) eligibility-state UI; (d) appeal scope clarification (sendback events only); (e) optional App-I-6.1 follow-up prompt; (f) App-I-7 spec corrections; (g) Phase B sign-off corrections. **After ideation closes:** the queued next paste is App-I-7 (Dispute Form + Appeal Feedback) — but with tightened scope post-ideation. May ship an App-I-6.1 first depending on ideation outcome. **App-I-6 ✅ approved 2026-05-12 LOCAL ONLY (code-wise; design follow-up needed)** — Per-incentive history view landed: NEW `lib/data/disqualifications.ts` (App-side mirror of Manager P-12 types + storage keys + 10 seeded disqualifications remapped to `currentDriver.id` 'driver-7821' + 6 seeded appeals + `seedTriggeringTripSnapshots` for synthetic trip context); NEW `lib/data/disqualification-helpers.ts` (`getDisqualificationsFor`, `getAppealForDisqualification`, `formatDisqualificationReason`, `formatDisqualificationLabel`, `getTriggeringTripSnapshot`, `useDisqualificationsState` hook with hydrate+persist+CRUD actions, `buildCountedTripRows` + `buildMissedOutRows` view-model helpers); NEW `app/incentives/[id]/history/page.tsx` route + NEW `components/driver/incentive-history-view.tsx` view (header card with title/bonus/status/date range + rolling-window chip; tab strip Counted | Missed Out (N); Counted = simple v3-style list with restored-from-appeal badge; Missed Out = desaturated cards with amber Disqualified badge + reason line + tap-to-expand inline driver-text/manager-reason on pending+denied appeals); EDIT `lib/data/incentive-utils.ts` (added `incentiveId` to `IncentiveProgressInfo` so the View history Link can route by id); EDIT `components/driver/dashboard-incentive-section.tsx` (outer `<button>` → `<div role="button">` to allow nested `<Link>`; added "View history →" Link with `stopPropagation` + `History` lucide icon; added bordered top divider row). Approved appeals move to Counted (restored badge); pending shows "Appeal under review" (blue); denied shows manager reason (expandable). No-appeal disqualifications show "Tap to dispute" — App-I-7 will replace the placeholder alert with `<DisputeAppealSheet>`. 7 files changed (4 new + 3 edits). Pairs with Manager P-12 ✅. v0 backfill pending.

> **Resume Wave context:** Phase B sequenced plan locked 2026-05-09 at `Project - Driver Incentives/v1/References/Resume Wave - Sequenced Plan.md`. **5 new App prompts queued (paired with Manager prompts):** App-I-3 (empty=All sync, pairs with P-10) → App-I-4 (v6 schema sync — sliding-window goal progress + new param reads, pairs with P-11) → App-I-5 (Dynamic Ends-in indicator, independent) → App-I-6 (Per-incentive history Counted⏐Missed Out + Disqualified-trip UI, pairs with Manager P-12) → App-I-7 (Dispute form + Appeal feedback, pairs with Manager P-13b). **Schema bump:** App reads v6 (single coordinated cycle from Manager P-11 + P-12). Driver Targeting catalog drops to 6 entries (Trips Completed, Days Since Last Activity, On-Time Pickup with window, Driver Address County, Tenure, Sendbacks with penalty/range/window). **Cross-prototype lockstep:** schema-touching pairs ship Manager-then-App with paired plan-sync-enforcer.

## Last Completed
✅ **App-I-6.2 — Driver Incentives Page Redesign (3-Section View + Filter Sheet + Layout v4)** (approved 2026-05-12 LOCAL ONLY; v0 backfill pending). **Multi-pass landing** — original Active|Past tab toggle reversed during user review; rebuilt as a single scrollable view on `/incentives` with three sections (In Progress / Earned / Recently Ended last 30 days), a header-anchored `<SlidersHorizontal>` filter button → bottom `<Sheet>` with Status radio group + Apply/Reset footer (future-proofed for new filter sections), and `<IncentiveCard>` reused across every section via new `isEnded` + `endedOutcome` props. Multi-pass UX polish landed same day: badge color reads `incentive.color` (matches detail view); two-button footer (`Your Rides` outline LEFT + `Available Rides·N` primary green RIGHT, equal flex-1, white count pill badge with green text); rolling-window date chip dropped; ends-in inline next to title (muted text calm, amber Hourglass pill urgent ≤7 days); `Temp disqualified` copy softened to `Action needed`; new `'missed-goal'` status pill (gray, MinusCircle); status pill collapses below progress when `status === 'in-progress'` (calm cards visibly shorter); rolling-window caption tightened to `4 of 5 in last 7 days`; detail rides view now shares the same `<IncentiveStatusPill>` + `incentive.color` as list (window-state Active/Ended pill dropped). New helpers in `lib/data/past-outcomes.ts`: `getPastEarnedFor`, `getRecentlyEndedFor`, `getPastIncentiveProgressInfo` adapter, `RECENTLY_ENDED_DAYS = 30`. `IncentiveStatus` extended with `'missed-goal'`. `formatEndsIn` past-date copy includes month+day. `getAllIncentiveProgress` filters past defs at source. Storage keys `:v6.1.1` → `:v6.2`. Paper-trail `dq-past-001` + `ap-past-001` (denied) seeded for the missed-criterion past outcome demo. Categorization for active items uses live `getIncentiveStatus` (not raw `isComplete`) — goal-hit-but-criterion-failing items stay in In Progress with their `Action needed` pill. **Files changed (~12 across the wave):** NEW `lib/data/past-outcomes.ts` · REWRITE `app/incentives/page.tsx` · EDIT `components/driver/dashboard-incentive-section.tsx` (IncentiveCard layout-v4 + two-button footer + chip simplification) · EDIT `components/driver/incentive-status-pill.tsx` · EDIT `components/driver/incentive-rides-view.tsx` · EDIT `components/driver/criterion-card.tsx` · EDIT `lib/data/disqualifications.ts` · EDIT `lib/data/incentives.ts` (4 past entries + Early Bird demo shift) · EDIT `lib/data/incentive-utils.ts` (formatEndsIn + getAllIncentiveProgress filter) · EDIT `lib/data/disqualification-helpers.ts` · DELETE `components/driver/past-incentive-row.tsx`. No schema bump. Verified in preview mobile 375x812 across all filter states.

✅ **App-I-6.1 + App-I-6.1.1 — Per-Incentive Rides View Reframe + List-View Status Pill** (approved 2026-05-12 LOCAL ONLY). App-I-6.1 dropped the `Counted ⏐ Missed Out` tab strip entirely; renamed route `/incentives/[id]/history` → `/incentives/[id]/rides`; CTA `View history →` → `View rides →`; renamed view component `<IncentiveHistoryView>` → `<IncentiveRidesView>`; added always-on Eligibility criteria block iterating `incentive.driverTargeting` with one `<CriterionCard>` per rule (✓/✗ state); `<DisqualifiedBanner>` above the block when any ✗ exists; 4-state appeal button per ✗ card (Appeal primary → placeholder alert; Appeal pending muted pill → placeholder alert; Appeal won green → opens shared `<AppealResultDialog>` with manager note; Appeal lost red → opens dialog with manager reason); approved appeal flips card ✗ → ✓ exempted, "Won" button retained as paper trail; rides list unconditional below criteria block; new helpers `getCriterionState` / `formatCriterionLabel` / `formatCriterionDetailLine`; deleted `buildMissedOutRows` + `MissedOutTripRow`. Inline scope extension App-I-6.1.1 added list-view `<IncentiveStatusPill>` driven by new `getIncentiveStatus` helper (4-state: in-progress / temp-disqualified / appeal-lost / earned); replaced legacy "Completed" green text. Seed refreshed for clean per-pill-state demos. 11 files changed (4 new + 7 edits). Pure App refactor; no schema bump. Storage keys bumped `:v6` → `:v6.1.1` to invalidate stale browser state.

✅ **App-I-6 — Per-Incentive History (Counted ⏐ Missed Out tabs) + Disqualified-Trip UI** (approved 2026-05-12 LOCAL ONLY; v0 backfill pending). Pairs with Manager P-12 ✅. **7 files changed (4 new + 3 edits).** **NEW `lib/data/disqualifications.ts`** — App-side mirror of Manager P-12 types (`Disqualification`, `Appeal`, `DriverTargetingType`, `TriggeringTripSnapshot`); storage keys `wingz-incentives:disqualifications:v6` + `wingz-incentives:appeals:v6` + reserved `wingz-incentives:appeal-acks:v1` (for App-I-7); **10 seeded disqualifications** ALL remapped to `currentDriver.id` ('driver-7821') so the per-incentive history view surfaces non-empty content under the App's logged-in-driver model (vs Manager's many-driver fleet); incentive IDs remapped to App's id-space (`inc-pp-001`, etc.); reason mix mirrors Manager P-12 (4 OTP + 4 Sendbacks + 2 DSLA); 6 seeded appeals (2 pending + 1 approved silent + 2 denied with realistic managerReason + 1 pending OTP); `seedTriggeringTripSnapshots` table provides denormalized trip context (date / pickup / dropoff) for synthetic `trip-dq-XXX` IDs so the Missed Out card renders rich row content without polluting seedTrips; `disqualificationDedupeKey` + `incentiveTypeForId` helpers. **NEW `lib/data/disqualification-helpers.ts`** — Pure helpers + persistence hook: `getDisqualificationsFor`, `getAppealForDisqualification`, `formatDisqualificationReason` (branches on OTP/Sendbacks/DSLA + fallback for the other 3 v6 types), `formatDisqualificationLabel` (short reason chip label), `getTriggeringTripSnapshot` (resolves real seed trip IDs `CURRENT-COMP-*` first, falls back to `seedTriggeringTripSnapshots`); `useDisqualificationsState` hook — lightweight client-side state (no context provider; mirrors Manager P-12's pattern) with hydrate-from-localStorage on mount + persist-on-change for each collection + `migrateDisqualification`/`migrateAppeal` defensive deserializers + CRUD actions exported for App-I-7 (`createAppeal`, `setAppealStatus`, `resolveDisqualification`, `clearAllAppeals`); `buildCountedTripRows` view-model helper combines (a) completed seed trips matching incentive type + (b) approved-appeal trips (Phase B locked semantic: "Approved appeal → trip moves to Counted automatically"), de-dupes by trip id with restored-from-appeal winning; `buildMissedOutRows` view-model helper excludes approved appeals (moved to Counted), sorts by createdAt desc, attaches resolved trip snapshot + reason copy + appealState discriminator (`'no-appeal' | 'pending' | 'denied'`) + appeal text + manager reason for inline rendering. **NEW `app/incentives/[id]/history/page.tsx`** — route handler that unwraps async `params` via React 19+ `use()`, looks up `IncentiveDefinition` by id, renders chrome (back arrow + "History" title) + `<IncentiveHistoryView>`. Not-found state surfaces a "Back to list" CTA. **NEW `components/driver/incentive-history-view.tsx`** — main view component: (1) header card with incentive Badge (per-incentive color via inline style fallback for arbitrary hex), status pill (Active/Upcoming/Ended), description, date range, rolling-window chip when applicable (ports App-I-4 explicit chip semantic), bonus amount on the right; (2) tab strip with TabButton component supporting count badge + warning variant (amber when Missed Out > 0); (3) Counted list — simple v3-style rows: date + pickup-time + pickup → dropoff truncated + per-trip bonus contribution + "Restored" badge for approved-appeal trips; empty state with check icon; (4) Missed Out list — disqualified-trip unique UI per W3-A2: desaturated card (`bg-gray-50 border-amber-200`), amber "Disqualified · <ReasonLabel>" badge with AlertTriangle icon, blue "Appeal under review" badge for pending, red "Appeal denied" badge for denied, date + pickup-time + pickup → dropoff, reason line, tap-to-expand inline detail (driver-text + manager-reason for pending/denied), "Tap to dispute" CTA hint for no-appeal. Tap handler: pending/denied → expand inline detail; no-appeal → opens placeholder alert (TODO App-I-7 wires `<DisputeAppealSheet>`). **EDIT `lib/data/incentive-utils.ts`** — extended `IncentiveProgressInfo` with `incentiveId: string` (canonical IncentiveDefinition.id, needed by the View history Link to route by id rather than type); set in `getIncentiveProgressInfo`. **EDIT `components/driver/dashboard-incentive-section.tsx`** — outer wrapper `<button>` → `<div role="button" tabIndex={0} onClick={...} onKeyDown={Enter/Space}>` so a nested `<Link>` doesn't produce invalid HTML (button-in-button); added "View history →" affordance under a bordered top divider with `History` lucide icon + double `e.stopPropagation()` on click+keydown so it doesn't trigger the outer tap-to-filter behavior. Existing tap-to-filter `/requests?incentive=...` preserved verbatim. **Acceptance verified:** ✅ Tap target on incentive card opens history view (8/8 cards have working links across dashboard + `/incentives` page). ✅ Tab strip switches Counted ⏐ Missed Out cleanly. ✅ Counted shows simple list of qualifying trips (per incentive). ✅ Missed Out shows desaturated cards + amber badge + reason. ✅ Pending appeals → "Appeal under review" badge inline. ✅ Approved appeals move to Counted (smoke-test: White Glove dq-003 approved → 0 Missed Out rows + 1 Restored row in Counted). ✅ Denied appeals stay in Missed Out with manager reason expandable. ✅ Existing tap-to-filter-Requests on card body still works. ✅ TypeScript clean (`tsc --noEmit` no diagnostics). ✅ HTTP 200 across `/`, `/incentives`, `/incentives/inc-pp-001/history`, `/incentives/inc-ww-001/history`, `/incentives/inc-hh-001/history`. **Smoke-test results:** seed counts 10/6 ✅ · reason copy formatted correctly per type (OTP, Sendbacks, DSLA) ✅ · trip snapshot lookup resolves real seed IDs + synthetic IDs ✅ · Peak Performer Missed Out shows 2 pending rows ✅ · White Glove Missed Out 0 rows / Counted 1 restored row ✅ · Hometown Hero Missed Out 2 rows (1 denied + 1 no-appeal) ✅.

✅ **App-I-5 — Dynamic Ends-in Indicator on `<IncentiveCard>`** (approved 2026-05-12 LOCAL ONLY; v0 backfill pending). Independent App feature (no Manager pair). **Helper:** new `formatEndsIn(endDate: Date | string, today?: Date): { copy: string; tone: 'urgent' | 'neutral' | 'ended' }` in `lib/data/incentive-utils.ts`. Date math: `Math.ceil((endDate - today) / day)`. Tone buckets: `urgent` when `daysUntilEnd <= 7` (amber chip "Ends in N day(s)"), `neutral` when `> 7` (muted chip "Ends MMM DD"), `ended` when `<= 0` (gray chip "Ended" — defensive; incentive shouldn't surface but render gracefully). Accepts `Date | string` for `endDate` because App stores window dates as ISO strings (per Schema Sync Note). Reuses existing `formatRollingWindowDateShort` helper internally for the "MMM DD" format. **Render:** `<IncentiveCard>` in `components/driver/dashboard-incentive-section.tsx` — chip row under the progress bar combines (a) the existing rolling-window date chip from App-I-4 (rolling-window mode only) + (b) the new ends-in chip (renders for every surfaced incentive). Flex-wrap (`flex flex-wrap items-center gap-1.5`) keeps the row intact on narrow screens. `Clock` lucide icon leads the ends-in chip. Tone styling via `cn()`: urgent `border-amber-300 bg-amber-50 text-amber-700`; neutral `border-gray-200 bg-gray-50 text-gray-600`; ended `border-gray-300 bg-gray-100 text-gray-500`. Used on dashboard carousel + `/incentives` page (same shared `<IncentiveCard>` component). NOT on ride-card pills (those don't show end dates). **Acceptance:** ✅ All 8 active cards show indicator on both surfaces. ✅ `daysUntilEnd <= 7` → amber + relative copy. ✅ `daysUntilEnd > 7` → neutral + absolute copy. ✅ Helper unit-testable in isolation (pure function over `Date | string` input). ✅ Existing card layout intact (CTA pill below, bonus + chevron right column unchanged). **Files changed (2):** `lib/data/incentive-utils.ts` (new helper + docblock), `components/driver/dashboard-incentive-section.tsx` (chip row wrapper + import). No schema change — reads `endDate` already on v6 schema. **Verification:** HTTP 200 on `/` + `/incentives`; TypeScript clean; sanity-checked tone branching via node REPL (1d → "Ends in 1 day" urgent; 7d → urgent; 8d → neutral "Ends MMM DD"; 0d → "Ended"). With today=2026-05-12 + seed endDates 2026-05-31 (most cards) + 2026-07-31 (Loyalty Streak + Long Haul), all current seed cards render neutral. To preview amber treatment, move any `endDate` within 7 days of `today`.

✅ **App-I-4 — v6 Schema Sync + Goal Modes + Targeting Param Extensions + Explicit Rolling-Window Date Display** (approved 2026-05-12 LOCAL ONLY; v0 backfill pending). Cross-prototype lockstep with Manager P-11 + P-11.1 ✅ complete. **App schema caught up v3 → v6 in one prompt** (v4 was Manager-only; v5 schedule model + Driver Targeting catalogue work was deferred via Resume Wave absorption). **Changes:** (1) `Goal` discriminated union ported from Manager — `goal: number` → `goal: { type: "total"; count: number } | { type: "rolling-window"; count: number; days: number }`. (2) Drop legacy `timeframe` + `enabled`; add `startDate` + `endDate` ISO datetime strings. (3) `IncentiveProgressInfo` extended with `goalMode` + `goalDays?` + `startDate` + `endDate` so display layer can render mode-aware captions + the explicit rolling-window date chip. (4) NEW `formatRollingWindow(goal, startDateIso, endDateIso, today)` helper — ported from Manager P-11.1 (signature accepts ISO strings since App stores dates as strings). (5) NEW `computeCurrentWindowProgress(type, count, days, startDateIso, endDateIso, today)` helper — counts qualifying trips in the **current** Y-day window (`[today - (days-1), today]`, clamped to startDate). **Semantic locked per user direction 2026-05-12:** the App shows "Current Y-day window: X done · N needed" — NOT "Best Y-day window so far" (driver-facing metric, not eval question). Window slides forward each day. On completion, existing `progress.isComplete` badge + opacity treatment serves as the lock (future enhancement freezes the chip to the completion-window snapshot — deferred from V1; would require `completedWindowFromIso`/`ToIso` field on `DriverIncentiveProgress`). (6) `<IncentiveCard>` (dashboard carousel + `/incentives` list): mode-aware `<ProgressMeter>` captions + **explicit rolling-window date chip** rendered as a bordered green chip with `CalendarRange` lucide icon directly under the progress bar (`border-[#10B981]/30 bg-[#10B981]/5 text-[#10B981]`). Treatment intentionally more prominent than Manager preview's muted caption per user direction. (7) `<ProgramContributionIndicator>` popover (ride-card pill tap): mode-aware caption + fix pre-existing stale `progress.targetCount` ref (left over from I-1's `targetCount → goal` rename; missed in grep sweep). (8) `<IncentiveEarnedPopup>`: fix pre-existing stale `data.def.name` ref (same I-1 rename pattern; missed grep sweep). **Re-seed:** 8 incentives — 6 total mode + 2 demo rolling-window (Peak Performer 5-in-7d, Quick Wins 5-in-7d) mirroring Manager P-11 demo picks. **Files changed (6):** `lib/data/incentives.ts` (schema bump + re-seed), `lib/data/incentive-utils.ts` (helpers + mode-aware progress), `components/driver/dashboard-incentive-section.tsx` (IncentiveCard rolling-window chip + mode-aware caption), `components/driver/program-contribution-indicator.tsx` (bug fix + mode-aware popover), `components/driver/incentive-earned-popup.tsx` (bug fix), `PROTOTYPE-BIBLE.md` (schemaVersion v3 → v6 + docblock). **V1 simplifications:** OTP `windowDays` + Sendbacks `penalty` informational at predicate layer (Driver schema carries 30-day aggregates only); `computeCurrentWindowProgress` falls back to seeded `currentCount` when live computation returns 0 (sparse seed trips). **Verification:** HTTP 200 across `/`, `/incentives`, `/requests`. schemaVersion reads `2026-05-12-v6`.

✅ **App-I-3 — Empty Market/Client Scope Read-Side Confirmation** (approved 2026-05-12 LOCAL ONLY). **Audit outcome: PASS — no behavior change required.** App has NO eligibility filter on `marketScope`/`clientScope`: (a) `lib/data/incentive-utils.ts::getAllIncentiveProgress()` maps every enabled incentive regardless of scope; (b) no helper anywhere reads `marketScope` or `clientScope` (codebase grep clean); (c) `CurrentDriver` carries no `market` or `client` field (slimmed in I-0); (d) zero UI references to "no markets" / "no clients" / "All Markets" / "All Clients" in App. Empty arrays already behave correctly — driver-side visibility is unaffected by Market/Client scope in v1 (intentional: scope is admin-side analytics, not driver-side gating; flag this in v1 PRD). **Code change:** added defensive block comments on `IncentiveDefinition.marketScope` + `clientScope` in `lib/data/incentives.ts` documenting (1) empty-as-All semantic from Manager P-10, (2) App's intentional passthrough today, (3) length-zero filter shortcut pattern (`array.length === 0 || array.includes(driver.market)`) for future maintainers. **Cross-prototype lockstep with Manager P-10 ✅ complete.** Manager actively interprets empty-as-All in UI labels + form validation + scope chips; App passively honors it by not filtering — different mechanisms, same observable outcome.

✅ I-1 Schema migration (approved 2026-05-05). Standalone I-2 Polish formally absorbed into Resume Wave (no longer ships as a discrete step; polish concerns redistributed across App-I-3..App-I-7 + final wave-induced flags into Manager I-7).

## v1 End-State Reference
The v1 BIBLE describes the END STATE. After I-0 through I-2 complete, the prototype should match the BIBLE.

## Build Queue

| # | Goal | Status | Description |
|---|---|---|---|
| 0 | Re-orientation | ✅ Approved | On the duplicated v0 chat: upload v1 BIBLE + TRACKER (overwriting v3 versions in repo). v0 confirms v1 scope. No code changes. |
| I-0 | Mega Strip — Variant Toggle + Tier + Leaderboard + `/payout` page + UpcomingPayoutWidget DELETED + Earned popup downgrade | ✅ Approved 2026-05-04 | One pass. DELETE: variant toggle infra, unused ride-card + dashboard variants, tier system, leaderboard, `/payout` page, **UpcomingPayoutWidget (component file + dashboard mount; revised 2026-05-04 from "downgrade" to full delete since all payout surfaces defer to v2)**, `Trip.revenueAddons`. STRIP: tier-coupled fields from schema (`tierLevel`, `INCENTIVE_TIER_BONUSES`, `Tier`, `TierConfig`, `LeaderboardEntry`). DOWNGRADE: Earned popup → single "Dismiss" CTA. STRIP: `/incentives` Tabs. |
| I-1 | Schema migration — per-incentive admin fields + multi-incentive Trip + binary progress + re-seed | ✅ Approved 2026-05-05 | ADD: `color`, `timeframe`, `enabled`, `sortOrder`, `marketScope`, `clientScope`, `trigger` to `IncentiveDefinition`. RENAME: `targetCount` → `goal`, `name` → `title`, `Trip.incentiveType` → `Trip.incentiveTypes` (array). DROP: `scheduledCount`, `periodId`. `bonusAmount` is sole $ source. Re-seeded 8 incentives + ~15 trips with multi-incentive examples. Pill bg uses `color`. Sort by `sortOrder` ASC. Binary progress UI ("X done · Y to go"). |
| I-2 | ~~Polish + edge states + final QA grep sweep~~ | 🟡 ABSORBED into Resume Wave 2026-05-09 | Standalone I-2 retired. Polish concerns redistributed: empty states + disabled-incentive filtering + [DEV] triggers fold into App-I-3/App-I-5 polish; final grep sweep folds into Manager I-7 cross-prototype final pass. |
| **App-I-3** | **Resume Wave: Empty Market/Client Scope Read-Side Confirmation** | ✅ Approved 2026-05-12 LOCAL ONLY | **Audit-only, comment-only change.** App has NO read-side filter on `marketScope`/`clientScope` (no helper consumes them; `CurrentDriver` has no market field; dashboard renders every enabled incentive regardless of scope). Empty arrays already work correctly because nothing reads them — pure passthrough. **Code:** added defensive block comments on `IncentiveDefinition.marketScope` + `clientScope` in `lib/data/incentives.ts` (empty-as-All from Manager P-10 + passthrough note + length-zero shortcut pattern for future maintainers). Cross-prototype lockstep with Manager P-10 complete. v0 backfill pending. |
| **App-I-4** | **Resume Wave: v6 Schema Sync — Goal Modes + Targeting Param Extensions + Explicit Rolling-Window Date Display** | ✅ Approved 2026-05-12 LOCAL ONLY | **Landed 2026-05-12.** Single coordinated v3 → v6 catch-up on App-side (v4 was Manager-only; v5 schedule model + Driver Targeting deferred via Resume Wave absorption). Goal discriminated union ported from Manager; `timeframe`/`enabled` dropped → `startDate`/`endDate` ISO strings added; `IncentiveProgressInfo` extended with `goalMode` + `goalDays?` + window dates; new `formatRollingWindow` + `computeCurrentWindowProgress` helpers (current window, NOT "best window so far" — corrected mid-prompt per user direction); `<IncentiveCard>` mode-aware progress + EXPLICIT rolling-window date chip (bordered green with calendar icon, more prominent than Manager preview); `<ProgramContributionIndicator>` popover mode-aware + pre-existing `.targetCount` → `.goal` bug fix; `<IncentiveEarnedPopup>` pre-existing `.def.name` → `.def.title` bug fix. **Re-seed:** 8 incentives — 6 total mode + 2 demo rolling-window (Peak Performer + Quick Wins, mirrors Manager P-11 picks). V1 simplifications: OTP `windowDays` + Sendbacks `penalty` informational at predicate layer; live-window count falls back to seeded `currentCount` if compute returns 0. **6 files changed.** Cross-prototype lockstep with Manager P-11 ✅ complete. v0 backfill pending. |
| **App-I-5** | **Resume Wave: Dynamic Ends-in Indicator on Incentive Card** | ✅ Approved 2026-05-12 LOCAL ONLY | **Landed 2026-05-12.** New helper `formatEndsIn(endDate, today): { copy, tone: 'urgent' \| 'neutral' \| 'ended' }` in `lib/data/incentive-utils.ts` — `urgent` (≤7d, amber chip), `neutral` (>7d, muted chip), `ended` (≤0d, gray defensive). Accepts `Date \| string` (App stores ISO strings). Chip rendered on `<IncentiveCard>` in a flex-wrap row under the progress bar; sits beside the rolling-window date chip from App-I-4 when both apply. `Clock` lucide icon leads the chip. Applies on dashboard carousel + `/incentives` page (same component). No schema change — reads existing `endDate`. 2 files changed (helper + IncentiveCard). v0 backfill pending. |
| **App-I-6** | **Resume Wave: Per-Incentive History (Counted ⏐ Missed Out tabs) + Disqualified-Trip UI** | ✅ Approved 2026-05-12 LOCAL ONLY · 🟡 reframed by App-I-6.1 (post-compact ideation 2026-05-12) | **Landed 2026-05-12 (code-wise).** 🔴 User flagged 7 framing issues during review (see WP §6 POST-COMPACT IDEATION QUEUE + Approval Log entry). Code stays at commit `10a8692` but Counted ⏐ Missed Out framing rejected for non-sendback reason types; redesign post-compact. Pairs with Manager P-12 ✅. 7 files (4 new + 3 edits). NEW `lib/data/disqualifications.ts` (App-side mirror of P-12 types; storage keys `:disqualifications:v6` + `:appeals:v6`; 10 seeded disqualifications all for currentDriver 'driver-7821'; 6 seeded appeals; `seedTriggeringTripSnapshots` for synthetic trip context); NEW `lib/data/disqualification-helpers.ts` (`getDisqualificationsFor`/`getAppealForDisqualification`/`formatDisqualificationReason`/`formatDisqualificationLabel`/`getTriggeringTripSnapshot`/`useDisqualificationsState` hook with hydrate+persist+CRUD/`buildCountedTripRows`/`buildMissedOutRows`); NEW `app/incentives/[id]/history/page.tsx` route + NEW `components/driver/incentive-history-view.tsx` view (header card + tab strip + Counted simple-list + Missed Out desaturated cards with amber Disqualified badge + reason line + inline expandable driver-text/manager-reason on pending+denied). EDIT `lib/data/incentive-utils.ts` (added `incentiveId` to `IncentiveProgressInfo`). EDIT `components/driver/dashboard-incentive-section.tsx` (outer `<button>` → `<div role="button">` to allow nested `<Link>`; added "View history →" affordance with stopPropagation). Approved appeals move to Counted (Restored badge); pending → "Appeal under review" blue badge; denied → manager reason expandable. No-appeal → "Tap to dispute" placeholder (App-I-7 replaces with `<DisputeAppealSheet>`). v0 backfill pending. Higher-thinking model. (was: ⬜ Planned Resume Wave 2026-05-09; reads v6 disqualifications; pairs with Manager P-12) | **Merged feature** combining intake W2-2 (tap incentive → see completed qualifying rides) + W3-A1 (disqualified-trips view) + W3-A2 (disqualified-trip unique UI). New tap target on dashboard `<IncentiveCard>` opens a per-incentive history view: tab strip "Counted ⏐ Missed Out (N)" where N = count of disqualifications for this driver+incentive. Counted tab = simple list (NOT full RideCard chrome — lighter format per user direction; reuses v3 simple-list pattern when user provides reference code) of trips that qualified. Missed Out tab = list of trips that triggered disqualification, each rendered with **disqualified-trip unique UI** (W3-A2): desaturated card + amber/red "Disqualified" badge + brief reason line ("OTP dropped to 92% — required 95%"). Tap on a Missed Out trip → opens dispute form (App-I-7). New routes: `app/incentives/[id]/history/page.tsx` OR inline-expand below the dashboard card — Phase D builder picks based on v3 reference code shape. **Disambiguation:** new tap target on card needs to coexist with existing "tap to filter Requests" behavior; chevron/affordance for history view, pill or progress-bar tap remains "filter Requests." Phase D locks affordance. Higher-thinking model. |
| **App-I-6.1** | **NEW 2026-05-12 (post-compact ideation): Per-Incentive Rides View + Always-On Criteria List + 4-State Appeal Button** | ✅ Approved 2026-05-12 LOCAL ONLY (inline scope extension App-I-6.1.1 also landed — see below) | Refactor on top of App-I-6 commit `10a8692`. **Drops tabs entirely.** Route rename `/history` → `/rides`; CTA `"View history →"` → `"View rides →"`. View component `<IncentiveHistoryView>` → `<IncentiveRidesView>`. NEW always-on **Eligibility criteria block** — iterates `incentive.driverTargeting`, renders `<CriterionCard>` per rule with ✓/✗ state. When any ✗ exists → amber `<DisqualifiedBanner>` above: `"Temporarily disqualified — appeal a criterion below to request a review. Trips continue to count toward your goal."` **4-state appeal button per ✗ card** (state machine derived by new `getCriterionState` helper): `Appeal` (primary → opens sheet placeholder; App-I-7 wires create mode) / `Appeal pending` (muted pill → opens sheet placeholder; App-I-7 wires view-pending mode) / `Appeal won` (green outline; card flips ✗ → ✓ exempted; tap opens `<AppealResultDialog>` with manager note) / `Appeal lost` (red outline; card stays ✗; tap opens `<AppealResultDialog>` with manager reason). Disqualified banner hides when no ✗ remain (after approvals). NEW helpers: `getCriterionState(rule, disqualifications, appeals, driverId, incentiveId)`, `formatCriterionLabel(rule)`, `formatCriterionDetailLine(rule, computedValues?)`. Delete `buildMissedOutRows`. Rides list section below criteria block, unconditional — every qualifying trip listed; drop "Restored" badge entirely (criterion card carries appeal state instead). `<AppealResultDialog>` minimal impl in App-I-6.1; App-I-7 refines + adds sheet modes. Seed coverage: ensure `currentDriver.id` ('driver-7821') has 1 incentive with no-appeal ✗, 1 with pending ✗, 1 with approved ✓-exempted, 1 with denied ✗, plus all-✓ incentives elsewhere. **No schema change** — reads existing v6 collections. Pairs with nothing (pure App refactor); tightens App-I-7 scope. Higher-thinking. |
| **App-I-6.1.1** | **NEW 2026-05-12 inline scope extension to App-I-6.1: List-view status pill + seed mix refresh** | ✅ Approved 2026-05-12 LOCAL ONLY | Replaces inline "Completed" green text on `<IncentiveCard>` with a 4-state pill (no pill = In progress / amber = Temp disqualified / red = Appeal lost / emerald = Earned). New `getIncentiveStatus(...)` helper derives state from goal progress + per-criterion eligibility. New `<IncentiveStatusPill>` component. Seed refreshed for clean per-state demos: dq-008 dropped (Early Bird becomes all-✓ "In progress"); White Glove progress bumped 3 → 6/6 (Earned demo, OTP exempted via approved appeal); Loyalty Streak retains "Temp disqualified" amber even at 5/5 goal (goal-hit-but-ineligible edge case). 3 files changed (helper extension + new component + dashboard-incentive-section.tsx CTA + seed 2 edits). Pure inline scope extension on top of App-I-6.1; no schema change. |
| **App-I-6.2** | **NEW 2026-05-12 (post-compact ideation): Driver-Level Past Incentives Archive (Past segment on `/incentives` page)** | ⬜ Planned (Higher-thinking; ships after App-I-6.1) | Adds **Active \| Past** segment toggle at the top of `/incentives` page. Active tab = existing list (current + upcoming incentives). Past tab = list of ended incentives the driver participated in. Each past row: incentive name + color chip + outcome badge (`Earned +$X` green / `Missed — goal not reached` gray / `Missed — appeal lost` red) + date range. Tap row → opens `/incentives/[id]/rides` in its naturally-frozen end state (criteria block shows final ✓/✗ states + won/lost buttons as paper trail; rides list is final; no "Appeal" primary CTA on ended campaigns — only won/lost result buttons + pending pill if still pending). Seed `seedPastOutcomes: { incentiveId, driverId, outcome: 'earned' \| 'missed-goal' \| 'missed-criterion', amountEarned?, missedReason? }[]` table parallel to seed incentives. Add 3–5 ended incentives to App seed (status `ended` via `deriveStatus`) + corresponding `seedPastOutcomes` rows for `currentDriver.id`. **No schema change** — seed-only additions + new component. Higher-thinking. |
| **I-7.5** | **Cross-Prototype Component Unification (Bun workspace; driver-facing only; nice-to-have)** | ⬜ Planned (added 2026-05-12 per user direction post-P-11.1; ships AFTER App-I-7) | **Cross-prototype prompt** — primary work happens at the project root (Bun workspace setup) + on this App's working dir (relocating driver-facing components to `packages/shared-driver-components/`) + on the Manager working dir (swapping preview hand-mocks for shared component consumers). **In scope from this App:** move `<IncentiveCard>` + `<RideCard>` + pill row + `<AppealResultDialog>` (built by App-I-7) + `<DisputeAppealSheet>` (built by App-I-7) + supporting helpers (e.g., the `formatRollingWindow` port from App-I-4) into the shared package. Update App imports to consume from `@wingz-incentives/shared-driver-components`. **Sequence:** ships AFTER App-I-7 so all driver-facing components are at final shape (no re-porting). **Status:** nice-to-have; may defer to v1.1 if PRD work pressures it out. See Manager TRACKER + BIBLE Build Queue for the full I-7.5 spec. Higher-thinking model. |
| **App-I-7** | **Resume Wave: Dispute Form + Appeal Feedback (Approved / Denied + Re-entry Pop-up)** | ✅ Approved 2026-05-13 LOCAL ONLY | **Landed 2026-05-13.** 6 files (3 new + 3 edits). NEW `lib/data/appeal-acks.ts` (ack-state localStorage helpers); NEW `components/driver/dispute-appeal-sheet.tsx` (mode-aware shadcn `Sheet` bottom-side; criterion-level red-tinted pre-filled banner via `formatDisqualificationReason`; create mode = required reason `<Textarea>` min-10-chars + optional comments + Cancel/Submit; view-pending mode = read-only driver text + submitted date + Close; parent owns hook + passes `onSubmit(driverText)` so the rides view's criterion state machine sees the new appeal immediately without reload); REFINE `components/driver/appeal-result-dialog.tsx` (denied tone red → amber `AlertTriangle`; criterion-level body via new `incentiveTitle` prop — "{title} — the {label} requirement is exempted for this campaign." / "...disqualification stands."; new `<AppealReentryPopupHost>` export scans appeals for unacknowledged resolved appeals + FIFO opens by `resolvedAt`; Dismiss calls `acknowledgeAppeal(appeal.id)`); EDIT `components/driver/incentive-rides-view.tsx` (replaced placeholder alerts with real sheet open + plumbs `createAppeal` via `onSubmit`; passes `incentiveTitle` to result dialog; acks paper-trail Appeal won/lost taps); EDIT `app/page.tsx` (mounts `<AppealReentryPopupHost />`). Verified in preview 2026-05-13: create flow flips card Appeal → Appeal pending immediately; view-pending shows just-submitted text + date; re-entry pop-up cycles FIFO ap-past-001 → ap-004 → ap-002 (emerald approved) → ap-003 with correct criterion-level copy + tone; reload after all dismissed → no popup (acks persisted). No schema change. v0 backfill pending. Pairs with Manager P-13b ✅. Higher-thinking model. |
| **App-MVP-2** | **MVP Scope-Down Wave: Appeals Surgical Strip (App) + Re-vendor Manager Preview** | ✅ Approved 2026-05-14 LOCAL ONLY (execution-only — closes queued wave step; cross-prototype pair of Manager MVP-1; Auto model; v0 backfill pending) | **Phase A — App-side strip (6 deletes + 7 edits):** DELETED `lib/data/appeal-acks.ts` · `lib/data/disqualifications.ts` · `lib/data/disqualification-helpers.ts` · `components/driver/criterion-card.tsx` · `components/driver/dispute-appeal-sheet.tsx` · `components/driver/incentive-status-pill.tsx`. EDITED `app/page.tsx` (dropped `AppealReentryPopupHost` import + mount); `components/driver/incentive-rides-view.tsx` (major rewrite — replaced `buildCountedTripRows` with inline `seedTrips` filter; dropped Eligibility section, CriterionCard list, DisqualifiedBanner, DisputeAppealSheet state machine, all appeal state derivations; view = header card + Completed rides only); `components/driver/dashboard-incentive-section.tsx` (dropped `useDisqualificationsState`/`getIncentiveStatus`/`IncentiveStatusPill`/status-tone Completed Rides styling/AlertTriangle+Clock+XCircle icons; button = plain outline); `app/incentives/page.tsx` (dropped `useDisqualificationsState`/`getIncentiveStatus` hooks; derived status from `progress.isComplete` only); `lib/data/past-outcomes.ts` (dropped `"missed-criterion"` from `PastOutcomeType` union + dropped inc-past-004 appeal-denied seed); `lib/data/incentives.ts` (dropped `driverTargeting?` field + all `driverTargeting:[...]` seed array literals); `lib/data/incentive-utils.ts` NO-EDIT. **Phase B — Manager preview re-vendor** (Manager working dir): DELETED 6 vendored Appeals files under `v1/Incentives V1 CS Tool/components/preview/vendored/` + `_lib/`; re-vendored 5 components + 4 helpers from post-strip App canonical with `Source last synced: 2026-05-14`; Manager preview view consumers needed no edits. **Acceptance gate met:** grep both prototypes case-insensitive `appeal|disqualif` → zero active-code Appeals hits (remaining: MVP-1 strip docblocks + `match-drivers.ts:20` P-12 hook comment + unrelated `lib/agent-mock/drivers-data.ts` onboarding string in BOTH prototypes — driver onboarding feature, not Appeals subsystem). App `/incentives/[id]/rides` = header card + Completed rides only; App dashboard button = plain outline; Manager `/incentives/[id]` renders 3 tabs only; embedded driver-app preview pane post-strip. Schema untouched (`:v3` stays). Both dev servers HTTP 200. App side of MVP wave COMPLETE — lockstep returns to Manager-only for MVP-3..MVP-6. |

## Approval Log

- **2026-05-14 — App-MVP-2 (MVP Scope-Down Wave: Appeals Surgical Strip — App + Re-vendor Manager Preview) ✅ Approved LOCAL ONLY (execution-only — closes the queued wave step exactly per spec; cross-prototype pair of Manager MVP-1; no strategy adjustments; Auto model).** Second landing of the MVP Scope-Down Wave; closes the App side of the wave (App side COMPLETE — no further App prompts in v1 MVP queue). **Phase A — App-side strip (6 deletes + 7 edits):** DELETED `lib/data/appeal-acks.ts` · `lib/data/disqualifications.ts` · `lib/data/disqualification-helpers.ts` · `components/driver/criterion-card.tsx` · `components/driver/dispute-appeal-sheet.tsx` · `components/driver/incentive-status-pill.tsx`. EDITED (1) `app/page.tsx` — dropped `AppealReentryPopupHost` import + mount; (2) `components/driver/incentive-rides-view.tsx` — major rewrite: replaced `buildCountedTripRows` with inline `seedTrips` filter; dropped Eligibility section, CriterionCard list, DisqualifiedBanner, DisputeAppealSheet state machine, all appeal state derivations; view now = header card + Completed rides only; (3) `components/driver/dashboard-incentive-section.tsx` — dropped `useDisqualificationsState`/`getIncentiveStatus`/`IncentiveStatusPill`/status-tone Completed Rides button styling/AlertTriangle+Clock+XCircle status icons; button = plain outline neutral; (4) `app/incentives/page.tsx` — dropped `useDisqualificationsState`/`getIncentiveStatus` hooks; derived status from `progress.isComplete` only; (5) `lib/data/past-outcomes.ts` — dropped `"missed-criterion"` from `PastOutcomeType` union + dropped inc-past-004 appeal-denied seed; (6) `lib/data/incentives.ts` — dropped `driverTargeting?` field + all `driverTargeting:[...]` seed array literals + scrubbed App-I-6.1/6.1.1 appeal-related comment refs; (7) `lib/data/incentive-utils.ts` NO-EDIT (verified no appeal refs to remove). **Phase B — Manager preview re-vendor** (work on Manager working dir at `v1/Incentives V1 CS Tool/components/preview/vendored/`): DELETED `criterion-card.tsx` · `dispute-appeal-sheet.tsx` · `incentive-status-pill.tsx` + `_lib/appeal-acks.ts` · `_lib/disqualifications.ts` · `_lib/disqualification-helpers.ts` (6 files). RE-VENDORED 5 components from post-strip App canonical (`dashboard-incentive-section.tsx` · `ride-card.tsx` · `incentive-pill-renderer.tsx` · `program-contribution-indicator.tsx` · `revenue-display.tsx`) + 4 helpers (`_lib/incentives.ts` · `_lib/incentive-utils.ts` · `_lib/mock-trips.ts` · `_lib/phone.ts`); all re-vendored files carry `Source last synced: 2026-05-14` in their docblock. Manager preview view consumers in `components/preview/views/` NO edits needed — already clean of references to deleted vendored exports. **Verification (acceptance gate met):** grep both prototypes case-insensitive `appeal|disqualif` returns zero active-code Appeals hits — remaining hits are (a) MVP-1 historical strip-note docblocks (acceptable), (b) `lib/utils/match-drivers.ts:20` single comment "P-12 hook" reference, (c) unrelated `lib/agent-mock/drivers-data.ts` onboarding-context string "Background check returned disqualifying findings" in BOTH prototypes (driver onboarding feature, not Appeals subsystem; "disqualifying" doesn't match `Disqualification` word boundary). App `/incentives/[id]/rides` renders just header card + Completed rides — no Eligibility section, no criterion cards, no disqualified banners, no appeal buttons (verified via preview MCP DOM eval). App dashboard `<IncentiveCard>` "Completed Rides" button = plain outline neutral styling (verified visually via Manager embedded preview screenshot — same vendored canonical). Manager `/incentives/[id]` renders 3 tabs only (Details · Drivers · Trips); embedded driver-app preview pane renders the post-strip App version (NO "Action needed" banner, NO IncentiveStatusPill, NO Appeals red/amber/sky button variants). Schema untouched (storage key stays `:v3`; no v6 → v7 bump). Both dev servers return HTTP 200 across all touched routes (verified via fetch). **Combined queue state after App-MVP-2 ✅: 39 ✅ + 4 🔜 = 43 total** (Manager 29 ✅ + 4 MVP-wave queued = 33; App 10 ✅). Path C local-first; v0 backfill pending (matches all post-P-9 prompts). **🔜 Next paste = Manager MVP-3 (More Filters drawer respects column visibility; Manager-only; Auto model) on Manager working dir `v1/Incentives V1 CS Tool/` at `:3001`.**

- **2026-05-13 — ✅ App-I-7.1 fourth polish wave — "Appeal submitted" button + `<DisqualifiedBanner>` adopt status-aware tone + copy (folded into App-I-7 approval; no separate prompt).** User flagged two consistency gaps: (a) the "Appeal submitted" pill on `<CriterionCard>` was rendered in a neutral gray treatment while the dashboard status pill `appeal-under-review` is sky-blue — same state, two different visual vocabularies; (b) the `<DisqualifiedBanner>` always read "Temporarily disqualified" in amber even when every ✗ criterion had a denied appeal or only had pending appeals. **Changes:** (1) **`<CriterionCard>` "Appeal submitted" pill** restyled `border-gray-200 bg-gray-50 text-gray-700` → `border-sky-300 bg-sky-50 text-sky-700` + Clock icon `text-gray-500` → `text-sky-600`. Matches the `appeal-under-review` status pill (sky + Clock) so the criterion-level pill + dashboard pill share the same visual vocabulary. (2) **`<DisqualifiedBanner>` becomes status-aware** via a new `status: 'temp-disqualified' | 'appeal-under-review' | 'appeal-lost'` prop with branching tone + icon + copy: amber `AlertTriangle` "Temporarily disqualified — appeal a criterion below to request a review. Trips continue to count toward your goal." (any ✗ no-appeal exists) · sky `Clock` "Appeal under review — your appeal is awaiting manager review. Trips continue to count toward your goal." (only ✗ pending) · red `XCircle` "Appeal denied — the disqualification stands for this campaign." (any ✗ denied). Precedence mirrors `getIncentiveStatus`: denied > no-appeal > pending. (3) **`<IncentiveRidesView>`** derives the banner status from `criteriaStates` and passes it down. **2 files edited:** `components/driver/criterion-card.tsx` (pill restyle + banner expanded to 3 variants with `DisqualifiedBannerStatus` exported type) · `components/driver/incentive-rides-view.tsx` (compute `bannerStatus` + pass to banner). **Verified at `:3000`:** Peak Performer (pending only) → sky banner with Clock + correct "Appeal under review" copy + sky "Appeal submitted" pill on criterion card. Hometown Hero (denied) → red banner with `circle-x` + "Appeal denied — the disqualification stands for this campaign." Loyalty Streak (no-appeal DSLA) → amber banner with `triangle-alert` + original "Temporarily disqualified..." copy. No schema change. No console errors.

- **2026-05-13 — ✅ App-I-7.1 third polish wave — inside header mirrors outside `<IncentiveCard>` (campaign window + rolling-window UI conventions ported in; folded into App-I-7 approval; no separate prompt).** User flagged the per-incentive `<IncentiveRidesView>` header still rendered the legacy treatment (full date range "May 1, 2026 – Aug 1, 2026" subtitle + bordered green "Window: May 7 – May 13" chip) while the outside `<IncentiveCard>` had moved to a cleaner layout in App-I-6.2 (ends-in indicator + mode-aware ProgressMeter caption). This wave ports the outside conventions into the inside header so the two surfaces are visually consistent. **Changes inside `<IncentiveRidesView>` header card:** (a) Title pill + ends-in indicator now share row 1 via `flex-wrap items-center gap-x-2` — urgent ≤7d renders as amber `Hourglass` pill, neutral as muted "Ends MMM DD" text (uses the existing `formatEndsIn` helper). (b) **Removed full date-range subtitle** "May 1, 2026 – Aug 1, 2026" — ends-in carries urgency; full window dates aren't actionable at this surface. (c) **Added mode-aware `<ProgressMeter>`** (imported from `dashboard-incentive-section.tsx` — already exported) under the description; pulls `currentCount` / `goal` / `goalMode` / `goalDays` via `getIncentiveProgressInfo(incentive.type)`. Rolling-window mode renders "4 of 5 in last 7 days"; total mode renders "X done · Y to go". (d) **Removed bordered green `Window: ...` chip** + the `formatRollingWindow` import — the rolling-window caption embedded in the ProgressMeter now carries the sliding-window context. (e) Dropped now-unused `formatShortDate` local helper + `CalendarRange` icon import. **2 files edited:** `components/driver/incentive-rides-view.tsx` (header rewrite + imports cleanup) — no schema change. **Verified at `:3000`:** Peak Performer (rolling-window) → "Ends Aug 1" muted text + ProgressMeter 80% width + caption "4 of 5 in last 7 days" + no `Window:` chip + no full date range. Early Bird (total mode, ends-in-6-days urgent) → amber `Hourglass` "Ends in 6 days" pill + ProgressMeter 87.5% width + caption "7 done · 1 to go". Header now matches outside `<IncentiveCard>` row-for-row except no status pill (still suppressed per prior polish wave — criterion cards carry per-rule state).

- **2026-05-13 — ✅ App-I-7.1 second polish wave — "Completed Rides" rename + aggregate status pill removed from per-incentive view (folded into App-I-7 approval; no separate prompt).** Two further refinements from preview review: (1) **Dashboard footer button "Your Rides" → "Completed Rides"** (more concrete copy — describes the content, not ownership); matching aria-label rewrite + section header inside `<IncentiveRidesView>` renamed `"Your rides"` → `"Completed rides"` so list-view button + per-incentive page heading stay aligned. (2) **Removed aggregate `<IncentiveStatusPill>` from the rides-view header card** — once the driver is inside the per-incentive view, the criterion-card row directly below carries appeal state per rule, so the aggregate pill duplicated information already visible. Status pill stays on the list-view `<IncentiveCard>` since that's the entry-point signal. Cleaned up the now-dead status derivation (`progressRow` / `pastOutcome` / `liveStatus` / `displayStatus`) + 4 unused imports (`IncentiveStatusPill`, `seedPastOutcomes`, `getIncentiveStatus`, `driverIncentiveProgress`) from `incentive-rides-view.tsx`. **2 files edited:** `components/driver/dashboard-incentive-section.tsx` (button label + aria-label) · `components/driver/incentive-rides-view.tsx` (drop status pill + dead deriv + dead imports + section heading rename). Verified on `:3000`: dashboard list 5/5 sampled cards render "Completed Rides" label + matching aria-label; per-incentive `/rides` view header card has no status pill (only the per-incentive Badge); rides section heading reads "Completed rides". No console errors. No schema change.

- **2026-05-13 — ✅ App-I-7.1 polish pass — discoverability + naming + sheet unification (folded into App-I-7 approval; no separate prompt).** Three connected refinements after preview review of App-I-7: (1) **Promote "Your Rides" button** in the dashboard `<IncentiveCard>` footer so the action surface visually pairs with the status pill above it. When `status === 'temp-disqualified' | 'appeal-under-review' | 'appeal-lost'` the button takes the matching pill tone (amber / sky-blue / red) + leads with the same lucide icon (`AlertTriangle` / `Clock` / `XCircle`). Earned + in-progress + missed-goal keep the default neutral outline. Closes the discoverability gap where "Action needed" was visible but the path to fix it (tap Your Rides → review criteria + appeal) wasn't obvious. (2) **`Appeal pending` → `Appeal submitted`** rename on the muted `<CriterionCard>` pill — perfect-tense reads as "you've done your part" rather than "still waiting." Paired with a **5th `IncentiveStatus` value `'appeal-under-review'`** (sky-blue pill with `Clock` icon + copy "Appeal under review"). New precedence in `getIncentiveStatus`: denied → no-appeal → pending → all-✓. The split means a criterion that ONLY has pending appeals no longer triggers "Action needed" — the driver has already submitted and the manager owns the next move; mixed pending + no-appeal still falls through to "Action needed" because there's an unsubmitted appeal to act on. (3) **Unified `<DisputeAppealSheet>` to 4 modes** (`create` / `view-pending` / `view-approved` / `view-denied`); **deleted standalone `<AppealResultDialog>`**. All four surfaces now share the same Sheet shell + red-tinted criterion banner + "Your appeal" section; only the header subtitle + footer section differ (Manager note emerald in view-approved; Manager reason amber in view-denied; pending shows only the read-only appeal + Close). `<AppealReentryPopupHost>` moved into `components/driver/dispute-appeal-sheet.tsx` and now mounts the unified Sheet instead of the deleted dialog. `<CriterionCard>` consolidated to a single `onOpenAppealSheet(mode, state)` callback (dropped `onOpenAppealResult`). `<IncentiveRidesView>` collapsed two state objects into one. **5 files changed (1 deleted, 4 edited):** DELETE `components/driver/appeal-result-dialog.tsx` (functionality folded into the Sheet); EDIT `lib/data/disqualification-helpers.ts` (new `'appeal-under-review'` state + precedence split); EDIT `components/driver/incentive-status-pill.tsx` (new variant — sky-blue Clock); EDIT `components/driver/criterion-card.tsx` (pill rename + single callback; docblock matrix updated); EDIT `components/driver/dispute-appeal-sheet.tsx` (4-mode rewrite + manager response sections + `<AppealReentryPopupHost>` relocated here); EDIT `components/driver/incentive-rides-view.tsx` (single Sheet state, mode derived from CriterionState, drops result-dialog branch); EDIT `components/driver/dashboard-incentive-section.tsx` (promoted Your Rides button); EDIT `app/page.tsx` (host import path → dispute-appeal-sheet). **Verified in preview at `http://localhost:3000` 2026-05-13:** `/incentives` list — Weekend Warrior + Peak Performer + Squad Goals show sky-blue Clock-icon Your Rides (pending-only criteria → "Appeal under review"); Hometown Hero + Quick Wins show red XCircle Your Rides (denied → "Appeal lost"); Loyalty Streak shows amber AlertTriangle Your Rides (DSLA no-appeal → "Action needed"); White Glove + Early Bird + Past earned rows keep neutral outline. Per-incentive `/rides`: pending criterion → button reads "Appeal submitted" (renamed); tap opens unified Sheet view-pending (banner + Your appeal section + Close). White Glove "Appeal won" green outline → Sheet view-approved (Title "White Glove" / subtitle "Your appeal was approved" / red banner + gray Your appeal + emerald Manager note "Approved. This criterion is exempted for this campaign." + Dismiss). Hometown Hero "Appeal lost" red outline → Sheet view-denied (subtitle "Appeal denied" / amber Manager reason with persisted denial copy + Dismiss). All four modes share consistent shell. No console errors after recompile. HTTP 200 across dashboard + all `/rides` routes. **No schema change.** Classification: execution-only (UX polish on already-approved App-I-7) with one **strategy-impacting** ripple — new `IncentiveStatus` value `'appeal-under-review'` becomes part of the cross-prototype status vocabulary (App-side only today; Manager P-14 may want to surface the same axis when reviewing how many appeals are submitted vs unresolved).

- **2026-05-13 — ✅ App-I-7 (Resume Wave Dispute Form + Appeal Feedback + Re-entry Pop-up) Approved LOCAL ONLY (Path C; v0 backfill pending).** Higher-thinking. Cross-prototype lockstep with Manager P-13b ✅ complete. **6 files (3 new + 3 edits):** (1) NEW `lib/data/appeal-acks.ts` — append-only `string[]` localStorage at `wingz-incentives:appeal-acks:v1`; `getAcknowledgedAppealIds()` + `acknowledgeAppeal(id)`; defensive parse (non-string entries dropped); idempotent (already-acked = no-op). (2) NEW `components/driver/dispute-appeal-sheet.tsx` — shadcn `Sheet` bottom-side, mode-aware (`create` / `view-pending`). Header: incentive title + "Appeal a missed criterion" (create) / "Appeal pending review" (view-pending). Red-tinted pre-filled banner reads `formatDisqualificationReason(disqualification)` — criterion-level per design pivot. Create mode: `<Textarea>` reason (required, `MIN_REASON_LENGTH=10` trim-then-length validation, state-aware helper, aria-invalid on touched-empty, destructive border) + optional comments + Cancel ghost + Submit emerald primary (disabled while `reasonTooShort`); on submit calls parent-provided `onSubmit(driverText)` with `driverText = reason + "\n\n" + comments`. View-pending mode: read-only driver text + "Submitted MMM D · pending manager review" + Close outline. Local form state resets via `useEffect` when open=false. **Hook lifted to parent mid-prompt** — the sheet previously called `useDisqualificationsState()` directly, creating a separate state instance whose writes wouldn't reflect in the rides view's hook until reload; refactored to receive `onSubmit` from the parent so the criterion state machine flips immediately. (3) REFINE `components/driver/appeal-result-dialog.tsx` — denied tone red → **amber** (`bg-amber-100` ring + `AlertTriangle` icon + `text-amber-800` title + amber bordered manager-reason section); approved stays emerald. Headlines: "Your appeal was approved" / "Appeal denied". New `incentiveTitle` prop drives criterion-level body line — approved: "{title} — the {label} requirement is exempted for this campaign."; denied: "{title} — the {label} disqualification stands.". `handleDismiss` always calls `acknowledgeAppeal(appeal.id)`. NEW exported `<AppealReentryPopupHost>` — scans `appeals` for current driver where `status !== 'pending'` AND `id NOT IN acks`, sorts FIFO by `resolvedAt ?? createdAt` ascending, renders the head; on dismiss bumps a `scanTick` to re-scan for the next. Looks up `Disqualification.failedRule.type` → matches incentive's `driverTargeting[].type` → computes exact criterion label via `formatCriterionLabel` (defensive fallback to first rule). (4) EDIT `components/driver/incentive-rides-view.tsx` — placeholder alerts replaced with real `setAppealSheet({...})` state. Added `<DisputeAppealSheet>` mount with `onSubmit` plumbed to local hook's `createAppeal({ driverId: CURRENT_DRIVER_ID, incentiveId: incentive.id, disqualificationId: appealSheet.disqualification.id, driverText })`. Added `incentiveTitle={incentive.title}` to `<AppealResultDialog>` mount. Added `acknowledgeAppeal(state.appeal.id)` inside `handleOpenAppealResult` so paper-trail opens via "Appeal won/lost" buttons satisfy the ack gate. (5) EDIT `app/page.tsx` — mounted `<AppealReentryPopupHost />` after `<BottomNav />`; null-renders until candidate exists so safe unconditional mount. **Verified in preview at `http://localhost:3000` 2026-05-13:** Peak Performer view-pending sheet → banner "OTP dropped to 92% — required 95%." + submitted text "Two of my late pickups…" + "Submitted May 10 · pending manager review" ✅; Weekend Warrior create sheet → banner "OTP dropped to 87% — required 90%." + Textarea validation (<10 chars → disabled; ≥10 chars → enabled) + Submit → toast "Appeal submitted — Your appeal is pending review for Weekend Warrior." + sheet closes + card button flips Appeal → Appeal pending immediately ✅; re-tap pending pill → view-pending shows just-submitted text + "Submitted May 13 · pending manager review" ✅; dashboard re-entry pop-up FIFO cycle: ap-past-001 (Peak Performer — Late April denied amber) → ap-004 (Quick Wins denied amber "Sendbacks ≤ 3 per 30 days disqualification stands.") → ap-002 (White Glove approved emerald "OTP ≥ 98% over 30 days requirement is exempted for this campaign.") → ap-003 (Hometown Hero denied amber "Sendbacks ≤ 2 per 30 days disqualification stands.") → no more popups ✅; reload `/` after all dismissed → no popup re-fires (acks persisted) ✅. No console errors. **No schema change** — only writes appeals (`createAppeal` exposed since App-I-6) + writes acks to localStorage (key reserved in App-I-6, activated here). **Combined cross-prototype queue: 34 ✅ + 3 remaining** (Manager: P-14 NEW, I-7.5, I-7 = 3; App: 0). 🔜 NEXT: paste **P-14 NEW** (Manager Incentive Details Tab Restructure + Read-Only-by-Default Edit Gating; Auto model) on Manager working dir `v1/Incentives V1 CS Tool/` at `:3001`.

- **2026-05-12 (even later — post-pill-flash-fix) — ✅ App-I-6.1.1 storage-key bump patch.** Bumped `DISQUALIFICATIONS_STORAGE_KEY` + `APPEALS_STORAGE_KEY` `:v6` → `:v6.1.1` in `lib/data/disqualifications.ts` after user reported status pills flashing on initial render then disappearing on hydration. Root cause: SSR/hydration mismatch — server renders pills from seed; on browser mount `useDisqualificationsState` reads localStorage and overrides seed; any browser that had previously persisted an empty `[]` payload (e.g., via `clearAllAppeals` or partial write) would trust the empty data and silently revert all cards to ✓ post-hydration. Key bump invalidates stale browser state so the new key is absent → hook falls back to seed → pills stay rendered. Schema shape unchanged; only the storage version suffix changed. 1 file edited. Workaround for users with old `:v6` orphans: DevTools → Application → Local Storage → delete the old keys (cosmetic only, not required for the fix).

- **2026-05-12 (even later) — ✅ App-I-6.1 + App-I-6.1.1 inline scope extension landed LOCAL ONLY (Path C; v0 backfill pending).** **App-I-6.1 — Per-Incentive Rides View + Always-On Criteria List + 4-State Appeal Button** (Higher-thinking; refactor on top of App-I-6 commit `10a8692`). 8 files changed (3 new + 5 edits): NEW `components/driver/criterion-card.tsx` (`<CriterionCard>` + `<DisqualifiedBanner>`; 4-state appeal button — `Appeal` primary / `Appeal pending` muted pill / `Appeal won` green outline / `Appeal lost` red outline); NEW `components/driver/appeal-result-dialog.tsx` (shared `<AppealResultDialog>` with tone+copy switch on approved/denied; will be extended in App-I-7 for dashboard re-entry pop-up); NEW `app/incentives/[id]/rides/page.tsx` (renamed route — `/history` deleted); EDIT `components/driver/incentive-rides-view.tsx` (RENAMED from `incentive-history-view.tsx`; dropped tab strip; added always-on Eligibility criteria block iterating `incentive.driverTargeting`; unconditional rides list with no "Restored" badge; helpers: `getCriterionState` + `formatCriterionLabel` + `formatCriterionDetailLine`; deleted `buildMissedOutRows`); EDIT `lib/data/disqualifications.ts` (added `DriverTargetingRule` type); EDIT `lib/data/incentives.ts` (added `driverTargeting?: DriverTargetingRule[]` field on `IncentiveDefinition` — documented subset of Manager P-11 schema; seeded all 8 incentives with appropriate rules to cover all 4 appeal states); EDIT `components/driver/dashboard-incentive-section.tsx` (CTA copy `"View history →"` → `"View rides →"`; Link target → `/rides`; `History` icon → `ListChecks`). **App-I-6.1.1 inline scope extension (also landed):** added list-view status pill replacing inline "Completed" green text. 3 additional files changed: NEW `components/driver/incentive-status-pill.tsx` (4-state visual: no-pill / amber / red / emerald); EDIT `lib/data/disqualification-helpers.ts` (added `getIncentiveStatus` + `IncentiveStatus` type — precedence: denied > no-appeal/pending > all-✓; goal-hit + all-✓ = `earned`); EDIT `components/driver/dashboard-incentive-section.tsx` (wired `useDisqualificationsState` hook + `incentiveDefinitions.find(...)` lookup + `getIncentiveStatus` call + `<IncentiveStatusPill>` render alongside Badge); EDIT seed: dropped `dq-008` (Early Bird Sendbacks no-appeal) so Early Bird demos plain "In progress" no-pill state; bumped White Glove progress 3 → 6/6 + isComplete true + bonusEarned 50 so White Glove demos "Earned" pill (goal hit + OTP appeal approved → ✓ exempted). **Verification:** `bunx tsc --noEmit` clean. HTTP 200 across `/`, `/incentives`, all 8 `/incentives/inc-XX-001/rides` routes; 404 on old `/history` route. Pill counts on `/incentives` list: 1 In progress (Early Bird) + 4 Temp disqualified (Weekend Warrior, Peak Performer, Squad Goals, Loyalty Streak) + 2 Appeal lost (Hometown Hero, Quick Wins) + 1 Earned (White Glove). Per-incentive views confirmed: Early Bird → "Currently meeting" ✓ Met card; White Glove → "Currently meeting" ✓ Exempted + "Appeal won" button; rest preserve their respective appeal states. Loyalty Streak surfaces the goal-hit-but-ineligible edge case correctly (5/5 done + DSLA no-appeal → amber "Temp disqualified" pill, bonus text stays green for goal completion). 🔜 Next paste = **App-I-6.2** (Driver-Level Past Incentives Archive — Past segment on `/incentives` page; Higher-thinking).

- **2026-05-12 (later — post-compact) — 🟢 App-I-6 design ideation CLOSED; App-I-6.1 + App-I-6.2 queued as follow-up prompts (sync-only, no code changes; next paste = App-I-6.1).** Post-compact session ran the design ideation triggered by the 7 framing issues raised during App-I-6 review (see entry below). User pushed back on the initial "sendback-only-appeal" interpretation mid-ideation: appealability is **per missed criterion**, not pre-classified by rule type. **Locked design:** per-incentive view at `/incentives/[id]/rides` (route renamed from `/history`) renders the header card (unchanged from App-I-6) + an **always-on Eligibility criteria block** (one ✓/✗ card per Driver Targeting rule) + unconditional rides list below. When any ✗ exists → amber "Temporarily disqualified" callout banner appears above the block. Each ✗ card carries a contextual 4-state appeal button (`Appeal` primary → opens `<DisputeAppealSheet mode="create">` placeholder in App-I-6.1, App-I-7 wires; `Appeal pending` muted pill → opens `<DisputeAppealSheet mode="view-pending">` placeholder; `Appeal won` green outline → opens `<AppealResultDialog>` with manager note; `Appeal lost` red outline → opens dialog with manager reason). Approved appeal flips card ✗ → ✓ exempted; "Won" button retained as paper trail. Banner hides when no ✗ remain. Goal-progress vs eligibility independence achieved via spatial separation (header counter ticks regardless of eligibility; rides list shows every qualifying trip regardless). Earning the incentive at campaign end requires goal reached AND zero ✗ criteria (all blockers either met or appeal-approved). Historical "completed vs missed out" data moves OUTSIDE per-incentive view → **App-I-6.2: Past segment on `/incentives` page** (Active | Past toggle) with outcome badges (Earned +$X / Missed — goal not reached / Missed — appeal lost) + `seedPastOutcomes` table. **App-I-7 spec corrections folded in:** `<DisputeAppealSheet>` becomes mode-aware (`create` / `view-pending`); pre-filled banner reads from criterion (`failedRule` + `computedValues`) not a trip ID; `<AppealResultDialog>` is shared with App-I-6.1's won/lost button interactions. **Phase B sign-off items Superseded:** (a) "W2-2 + W3-A1 + W3-A2 merge into Counted ⏐ Missed Out tabs" → replaced by always-on criteria block + unconditional rides list (no tabs); (b) "Disqualification eval = LIVE per-trip" → partially superseded — eval mechanism unchanged from P-12 (still runs per-trip for ALL rule types), but UI surfaces at the **criterion** level, NOT the trip level. **Queue update:** +App-I-6.1 (per-incentive view reframe; Higher-thinking; pure App refactor on top of commit `10a8692`) + App-I-6.2 (driver-level past archive; Higher-thinking). Combined cross-prototype queue 28 ✅ + 7 remaining (App-I-6.1, App-I-6.2, P-13a, P-13b, App-I-7, I-7.5 NEW, I-7). **Cross-prototype scope:** Manager P-13a/P-13b unchanged (Manager retains all rule-type appeals as source of truth; driver-side filtering is App-only). Next paste = App-I-6.1 on App working dir `v1/Incentives V1 App/` at :3001. — Full ideation transcript captured in [Working Plan §6 Resolutions](../../../Working%20Plan%20-%20Driver%20Incentives.md#6-open-questions-need-decision) + [§10 top entry](../../../Working%20Plan%20-%20Driver%20Incentives.md#10-change-log-rolling).

- **2026-05-12 (post-review) — 🔴 App-I-6 design follow-up flagged during user review (sync-only, no code changes; ideation post-compact).** User reviewed the App-I-6 build (commit `10a8692`) and raised 7 framing issues that require post-compact redesign before App-I-7 ships. App-I-6 code remains in the repo and works mechanically (tap target on `<IncentiveCard>` opens history view; tab strip switches; localStorage round-trips; seed renders) — but the conceptual model is wrong for non-sendback reason types. **The 7 issues (verbatim from user direction):** (1) "View history" CTA → "View rides" (more concrete; what drivers actually see is rides, not abstract history); (2) "Missed or counted doesn't make sense from the dispute perspective. Because missing a specific achievement isn't because of one specific ride. Its because the user missed a specific criteria and not all triggers will have that state for example"; (3) "It could be the driver did not meet the goal within the campaign period" (goal-period expiry — no specific bad trip); (4) "The driver's best goal count within the rolling window wasn't enough" (rolling-window goal not met — also not a per-trip event); (5) "The Driver's OTP dipped below the requirement (And we can make it explicit that if it gets back up the OTP trips that they finish that will still be counted as long as they get back their OTP and finish the count within the timeframe)" — OTP is recoverable rolling state; (6) "You have to base it on the driver targeting options right now. The goal is that the driver during the campaign period is eligible and if they complete the goal within the campaign period while being eligible they will get the incentive. If they only partially completed it became ineligible during the campaign period they can do 2 things: (a) Do whatever you need to meet criteria (Complete enough trips till OTP is high enough for example) (b) Or appeal to get exempted from (For example they did sendback a trip disqualifying them but they had a valid reason)"; (7) **CRITICAL** — "Make sure that as long as the campaign is there and lets say they finished 5/8 and became unqualified and work towards qualifying or appealing any trips they take that still would increase the goal would be counted. They don't have to wait on being exempted or reaching the qualifications to continue building up toward the goal. So what could happen is the driver might be disqualified at 5/8. Appeal. During appeal process became 8/8, once appeal is lifted they already accomplished the goal. and make that apparent." **Classification:** strategy-impacting (eligibility model is baseline shift; appeal scope tightened to specific event types; goal-progress vs eligibility independence is a new product invariant). **Phase B sign-off items being marked Superseded:** (a) "W2-2 + W3-A1 + W3-A2 MERGE into single per-incentive history view with `Counted ⏐ Missed Out` tabs" — Superseded; (b) "Disqualification eval = LIVE per-trip" — Partially superseded (still applies for sendback events; aggregate states OTP/DSLA/goal-window are rolling-state evaluations). **No code changes this sync** — ideation happens post-compact. Sync receipt + Working Plan §6 "POST-COMPACT IDEATION QUEUE" + §10 entry + §11 Next Action + Resume Prompt + General Plan §5 App-I-6 row + Project Index banner + memory frontmatter + MEMORY.md index + this entry + sandbox cp re-mirror. **🔜 Next:** post-compact ideation session producing — (a) "View rides" CTA copy fix; (b) view-component redesign (likely Rides + Eligibility/Status); (c) eligibility-state UI making goal-progress vs eligibility independence apparent; (d) appeal scope clarification (sendback events only); (e) optional App-I-6.1 follow-up prompt body; (f) App-I-7 spec corrections; (g) Phase B sign-off corrections at `v1/References/Resume Wave - Sequenced Plan.md`. After ideation closes → paste P-13a (Manager Appeals Review Skeleton — read path only; Auto model) on Manager working dir at :3000. P-13a scope may need re-eval if the appeal model tightens.
- **2026-05-12 — App-I-6 (Resume Wave Per-Incentive History Counted ⏐ Missed Out tabs + Disqualified-Trip UI) ✅ Approved LOCAL ONLY.** Cross-prototype lockstep with Manager P-12 ✅ complete (same-day pairing). Merged feature combining W2-2 (tap-to-history of qualifying rides) + W3-A1 (disqualified-trips view) + W3-A2 (disqualified-trip unique UI). **7 files changed (4 new + 3 edits):** (1) **NEW** `lib/data/disqualifications.ts` — App-side mirror of Manager P-12 types: `Disqualification` (id, driverId, incentiveId, triggeringTripId, failedRule {type, params}, computedValues {observed, threshold, unit?}, createdAt string (App uses ISO string vs Manager's Date — JSON round-trip safer), resolved?, triggerSource?) + `Appeal` (id, driverId, incentiveId, disqualificationId, driverText, status: 'pending'|'approved'|'denied', managerReason?, createdAt string, resolvedAt? string) + `DriverTargetingType` string-literal union (aligned with Manager catalog) + `TriggeringTripSnapshot` (date, pickup, dropoff, pickupTime? — denormalized App-side display enrichment); storage keys `wingz-incentives:disqualifications:v6` + `wingz-incentives:appeals:v6` + reserved `wingz-incentives:appeal-acks:v1` (App-I-7 ack state); **10 seeded disqualifications** ALL with `driverId: 'driver-7821'` (App's currentDriver) — Manager-side variety remapped to App's single logged-in-driver model so the per-incentive history view surfaces non-empty content; incentive IDs remapped to App's id-space (`inc-pp-001` for Peak Performer, `inc-wg-001` for White Glove, `inc-ww-001` for Weekend Warrior, `inc-hh-001` for Hometown Hero, `inc-qw-001` for Quick Wins, `inc-eb-001` for Early Bird used in place of Manager's milestone-100, `inc-sg-001` for Squad Goals, `inc-ls-001` for Loyalty Streak); reason mix mirrors Manager P-12 (4 OTP + 4 Sendbacks + 2 DSLA); **6 seeded appeals** mirror Manager P-12 status distribution (2 pending + 1 approved silent + 2 denied with realistic managerReason + 1 pending OTP); `seedTriggeringTripSnapshots` table provides denormalized trip context (date, pickup, dropoff, pickupTime) for synthetic `trip-dq-001..010` IDs so the Missed Out card renders rich row content without polluting seedTrips; exports `disqualificationDedupeKey(driverId, incentiveId, failedRuleType): string` + `incentiveTypeForId(id, defs): IncentiveType | undefined`. (2) **NEW** `lib/data/disqualification-helpers.ts` — pure helpers + persistence hook: `getDisqualificationsFor(disqualifications, driverId, incentiveId): Disqualification[]` · `getAppealForDisqualification(appeals, disqualificationId): Appeal | undefined` · `formatDisqualificationReason(d): string` (branches on `failedRule.type` — OTP "OTP dropped to X% — required Y%.", Sendbacks "Sendbacks hit X in Y days — cap was Z.", DSLA "X days since last activity — required ≤ Y days.", with fallbacks for `tripsCompletedInWindow`, `addressCounty`, `tenureDays`) · `formatDisqualificationLabel(type): string` (short chip label: "OTP" / "Sendbacks" / "Inactivity" / "Trip count" / "Address" / "Tenure") · `getTriggeringTripSnapshot(triggeringTripId): TriggeringTripSnapshot | null` (resolves real seed trip IDs `CURRENT-COMP-*` / `COMP-2023-*` first via `seedTrips.find`, falls back to `seedTriggeringTripSnapshots` table for synthetic IDs); `useDisqualificationsState()` — lightweight client-side hook (no React context provider; mirrors Manager P-12's persistence pattern but tighter scope) returning `{isHydrated, disqualifications, appeals, createAppeal({...}): Appeal, setAppealStatus(appealId, status, managerReason?): void, resolveDisqualification(id): void, clearAllAppeals(): void}`; hydrate-from-localStorage on mount + persist-on-change useEffects for each collection; `migrateDisqualification`/`migrateAppeal` defensive deserializers (validate required fields + `Appeal.status` enum; reject malformed rows); `generateAppealId()` for new pending appeals (App-I-7 hookup); `buildCountedTripRows({incentiveType, incentiveId, driverId, bonusAmount, goalCount, disqualifications, appeals}): CountedTripRow[]` combines (a) `seedTrips.filter(t => t.status==='completed' && t.incentiveTypes.includes(type))` + (b) `appeals.filter(a => a.status==='approved')` resolved to their triggering trip snapshots (Phase B locked semantic: "Approved appeals → trip moves to Counted automatically"); de-dupes by trip id with `restoredFromAppeal: true` flag winning when both sources match; `buildMissedOutRows({driverId, incentiveId, disqualifications, appeals}): MissedOutTripRow[]` excludes disqualifications whose appeal has `status === 'approved'` (they moved to Counted); sorts by createdAt desc; attaches `appealState: 'no-appeal' | 'pending' | 'denied'` discriminator + appeal driver-text + manager-reason for inline rendering; exports `CURRENT_DRIVER_ID` convenience constant. (3) **NEW** `app/incentives/[id]/history/page.tsx` — route handler that unwraps Next.js 15 async `params` via React 19+ `use()`, looks up `IncentiveDefinition` by id from `incentiveDefinitions`, renders chrome (back arrow button + centered "History" title) + delegates to `<IncentiveHistoryView>`. Not-found state surfaces a "Back to list" CTA pointing to `/incentives`. (4) **NEW** `components/driver/incentive-history-view.tsx` — main view component (`'use client'`): header card with incentive Badge (per-incentive color via inline `style` fallback since arbitrary hex doesn't tailwind-compile), status pill (Active emerald / Upcoming blue / Ended gray — `deriveStatus` reads today vs startDate/endDate), description, date range ("May 1, 2026 – May 31, 2026" via internal `formatShortDate`), rolling-window chip via `formatRollingWindow` from App-I-4 (ports the explicit chip semantic — bordered green chip with `CalendarRange` icon), bonus amount on the right; tab strip with `TabButton` component supporting count badge + warning variant (amber when Missed Out count > 0); `CountedList` empty-state with `CheckCircle2`; `<CountedList rows>` renders simple v3-style rows: date + pickup-time + pickup → dropoff truncated + per-trip bonus contribution (`bonusAmount / goalCount`) + emerald "Restored" badge for `restoredFromAppeal` rows; `<MissedOutList rows>` empty-state ("No disqualified trips. Keep it up!"); `<MissedOutCard row>` desaturated card UI per W3-A2 — `bg-gray-50 border-amber-200`, amber "Disqualified · <ReasonLabel>" badge with `AlertTriangle` icon, blue "Appeal under review" badge with `Clock` icon for pending state, red "Appeal denied" badge with `MessageCircleWarning` icon for denied state, date + pickup-time + pickup → dropoff + amber reason line; tap behavior — pending/denied → expand inline detail showing driver appeal text + manager response (denied only) in nested cards; no-appeal → opens placeholder `alert()` with disqualification reason as a TODO marker for App-I-7's `<DisputeAppealSheet>` integration; aria-expanded set on the button when stateful. (5) **EDIT** `lib/data/incentive-utils.ts` — extended `IncentiveProgressInfo` interface with `incentiveId: string` (canonical IncentiveDefinition.id like `inc-pp-001`, needed by the View history Link to route by id rather than type since `IncentiveType` discriminator is variant-level not id-level); set `incentiveId: definition.id` in `getIncentiveProgressInfo`. (6) **EDIT** `components/driver/dashboard-incentive-section.tsx` — outer wrapper `<button onClick={onTap}>` → `<div role="button" tabIndex={0} onClick={handleCardTap} onKeyDown={handleCardKey}>` (Enter/Space invoke handler with preventDefault) so a nested `<Link>` doesn't produce invalid HTML (button-in-button); imports `Link` from `next/link` + `History` from `lucide-react`; added "View history →" Link under a bordered top divider row (`border-t border-gray-100`) at the bottom of the card; uses double `e.stopPropagation()` on `onClick` + `onKeyDown` so it doesn't trigger the outer tap-to-filter behavior; aria-label includes incentive name for screen readers. Existing tap-to-filter `/requests?incentive=...` flow on card body preserved verbatim. (7) [Cleanup note] No new ride-card or pill-renderer changes — those carry no end-date or disqualification surfaces. **Acceptance verified:** ✅ Tap target on incentive card opens history view (8/8 incentives have working links across dashboard + `/incentives` page; verified via curl scan — 8 unique `/incentives/inc-XX-001/history` hrefs). ✅ Tab strip switches Counted / Missed Out cleanly via `useState`. ✅ Counted tab shows simple list of qualifying trips (per incentive type from seed). ✅ Missed Out tab shows desaturated cards with amber Disqualified badge + reason line. ✅ Pending appeals surface "Appeal under review" blue badge inline. ✅ Approved appeals move to Counted (smoke-tested: White Glove dq-003 with approved appeal → 0 rows in Missed Out + 1 Restored row in Counted with `restoredFromAppeal: true`). ✅ Denied appeals stay in Missed Out with manager reason text shown on tap-to-expand. ✅ Existing tap-to-filter-Requests behavior on incentive card body still works (verified — outer `<div role="button">` preserves the onClick handler; only the nested Link stops propagation). ✅ TypeScript clean (`npx tsc --noEmit` no diagnostics on App side). ✅ HTTP 200 across `/`, `/incentives`, `/incentives/inc-pp-001/history`, `/incentives/inc-ww-001/history`, `/incentives/inc-hh-001/history`. **Smoke test outputs (`bun p-app-i6-smoke.ts`):** seed counts 10 + 6 ✅; reason copy formatted correctly per type ("OTP dropped to 92% — required 95%." / "Sendbacks hit 3 in 30 days — cap was 2." / "9 days since last activity — required ≤ 7 days.") ✅; trip snapshot lookup resolves real seed trip IDs + synthetic `trip-dq-XXX` IDs ✅; Peak Performer Missed Out 2 rows (dq-001 + dq-002 both pending) ✅; White Glove Missed Out 0 rows + Counted shows trip-dq-003 with `restoredFromAppeal: true` ✅; Hometown Hero Missed Out 2 rows (dq-005 denied + dq-006 no-appeal) ✅. **No-appeal tap action is a placeholder `alert()`** — TODO marker for App-I-7 (`<DisputeAppealSheet>` will replace this; the sheet uses `useDisqualificationsState().createAppeal(...)` already exported from the hook). **Combined cross-prototype queue: 28 ✅ + 5 remaining** (Manager: P-13a, P-13b, I-7.5, I-7 = 4; App: App-I-7 = 1). Doc updates (this sync): App TRACKER Current Step → App-I-7, Last Completed expanded with App-I-6 detail, Build Queue App-I-6 row flip ✅, Approval Log new top entry; App BIBLE Build Queue App-I-6 row flip ✅ + Resume Wave Reads § for Disqualification + Appeal Reads upgraded to ✅ LANDED; Manager TRACKER Current Step → App-I-7, Approval Log breadcrumb; this entry. Pre-paste sandbox cp sync to `Driver App/References/v0-repo-files/` pending in this sync pass.
- **2026-05-12 — App-I-5 (Resume Wave Dynamic Ends-in Indicator on `<IncentiveCard>`) ✅ Approved LOCAL ONLY.** Independent App feature (W2-1; no Manager pair). **2 files changed:** (1) `lib/data/incentive-utils.ts` — new `formatEndsIn(endDate: Date | string, today?: Date): { copy: string; tone: 'urgent' | 'neutral' | 'ended' }` exported helper. Date math: `Math.ceil((endDate - today) / day)`. Tone buckets: `'urgent'` when `daysUntilEnd <= 7` → copy `"Ends in N day(s)"` (singular "day" when N === 1); `'neutral'` when `daysUntilEnd > 7` → copy `"Ends MMM DD"` (e.g., "Ends May 31") using existing internal `formatRollingWindowDateShort` helper; `'ended'` when `daysUntilEnd <= 0` → copy `"Ended"` (defensive — incentive shouldn't surface but render gracefully if it does). Accepts `Date | string` for `endDate` because App stores ISO datetime strings (Schema Sync Note: Manager → `Date`, App → `string`). Bad-input guard (`Number.isNaN(end.getTime())`) returns `ended` rather than crashing. Header docblock extended with App-I-5 addition. (2) `components/driver/dashboard-incentive-section.tsx` — imported `formatEndsIn` + `Clock` lucide icon; computed `endsIn` from `progress.endDate` inside `<IncentiveCard>`; replaced the conditional rolling-window chip with a flex-wrap chip row (`mt-2 flex flex-wrap items-center gap-1.5`) that contains both chips. Rolling-window chip remains gated on `rollingWindow !== null` (rolling-window mode + today >= startDate); ends-in chip always renders for any surfaced incentive. Tone styling via `cn()`: urgent → `border-amber-300 bg-amber-50 text-amber-700`; neutral → `border-gray-200 bg-gray-50 text-gray-600`; ended → `border-gray-300 bg-gray-100 text-gray-500`. ARIA: `aria-label={endsIn.copy}` on the chip span. **Surfaces touched:** dashboard incentive carousel (`<DashboardIncentiveSection>` → `<IncentiveCard>`) + `/incentives` page list (imports same `<IncentiveCard>` from `dashboard-incentive-section.tsx`). NOT on ride-card pills (those don't show end dates — out of scope per task spec). **Acceptance verified:** ✅ All 8 active cards show ends-in chip on both surfaces (`curl http://localhost:3001/` → 16 chip instances; `/incentives` → 8 chip instances). ✅ Tone branching sanity-checked via node REPL (1d → "Ends in 1 day" urgent; 7d → urgent; 8d → neutral; 0d → "Ended"). ✅ Helper is pure + unit-testable in isolation. ✅ Existing card layout intact (badge row, description, progress bar, CTA pill, bonus + chevron right column unchanged). **Seed coverage note:** today=2026-05-12 + seed endDates 2026-05-31 (most cards, 19d out) + 2026-07-31 (Loyalty Streak + Long Haul, 80d out) → all current seed cards render neutral. To preview amber treatment, an `endDate` would need to fall within 7d of today; not in scope to seed since acceptance is about the helper + render path. **Verification:** HTTP 200 on `/`, `/incentives`, `/requests`; TypeScript clean (`npx tsc --noEmit` no diagnostics). **Combined cross-prototype queue: 26 ✅ + 7 remaining** (Manager: P-12, P-13a, P-13b, I-7.5, I-7 = 5; App: App-I-6, App-I-7 = 2). **🔜 NEXT: paste P-12 (Disqualification + Appeal Schema — foundational, no UI; Higher-thinking; pairs with App-I-6) on Manager working dir `v1/Incentives V1 CS Tool/` at :3000.** v0 backfill pending (v0 sandbox still broken — Path C local-first remains the rule).
- **2026-05-12 — App-I-4 (Resume Wave v6 Schema Sync — Goal Modes + Targeting Param Extensions + Explicit Rolling-Window Date Display) ✅ Approved LOCAL ONLY.** Cross-prototype lockstep with Manager P-11 + P-11.1 ✅ complete (Manager landed earlier the same day). **App caught up v3 → v6 in one prompt** (v4 multi-trigger composition was Manager-only; v5 schedule model + Driver Targeting catalogue work was deferred via Resume Wave absorption — App skipped straight to v6). **6 files changed:** (1) `lib/data/incentives.ts` — schema bump: added `Goal` discriminated union (ported from Manager 1:1); `IncentiveDefinition.goal: number` → `goal: Goal`; dropped `timeframe` + `enabled`; added `startDate` + `endDate` ISO datetime strings; re-seeded 8 incentives (6 total mode + 2 demo rolling-window: Peak Performer 5-in-7d, Quick Wins 5-in-7d — mirrors Manager P-11 demo picks); preserved App-only fields (`qualifyingCriteria`, `trigger`, `iconName`) per Schema Sync Note "documented subset" rule. (2) `lib/data/incentive-utils.ts` — extended `IncentiveProgressInfo` with `goalMode` + `goalDays?` + `startDate` + `endDate`; `getIncentiveProgressInfo` unpacks discriminated Goal; NEW `formatRollingWindow(goal, startDateIso, endDateIso, today)` helper ported from Manager P-11.1 (signature accepts ISO strings since App stores dates as strings); NEW `computeCurrentWindowProgress(type, count, days, startDateIso, endDateIso, today)` helper. **Semantic locked per user direction 2026-05-12 (mid-prompt correction):** "Current Y-day window: X done · N needed" — NOT "Best Y-day window so far". Driver-facing metric, not eval question. Window slides forward each day. On completion, existing `progress.isComplete` badge + opacity treatment serves as the lock; future enhancement freezes the chip to the completion-window snapshot (would require `completedWindowFromIso`/`ToIso` field on `DriverIncentiveProgress` — deferred from V1). (3) `components/driver/dashboard-incentive-section.tsx` — `<IncentiveCard>` mode-aware `<ProgressMeter>` + **EXPLICIT rolling-window date chip** rendered as bordered green chip with `CalendarRange` lucide icon directly under the progress bar (`border-[#10B981]/30 bg-[#10B981]/5 text-[#10B981]`). Treatment intentionally MORE prominent than Manager preview's muted caption per user direction. Null cases (total mode + upcoming) → no chip. (4) `components/driver/program-contribution-indicator.tsx` — mode-aware popover caption ("Current Y-day window: X done · N needed" for rolling-window) + **pre-existing stale ref bug fix:** `progress.targetCount` → `progress.goal` (left over from I-1's `targetCount → goal` rename; missed in original grep sweep — would have rendered `undefined`/NaN). (5) `components/driver/incentive-earned-popup.tsx` — **pre-existing stale ref bug fix:** `data.def.name` → `data.def.title` (same I-1 rename pattern, also missed grep sweep — would have rendered blank). (6) `PROTOTYPE-BIBLE.md` — `schemaVersion: 2026-05-05-v3` → `schemaVersion: 2026-05-12-v6` + full catch-up docblock. **V1 simplifications carried from Manager P-11:** OTP `windowDays` informational at predicate layer (Driver schema carries 30-day snapshot only); Sendbacks `penalty` filter informational (driver carries raw count only); `computeCurrentWindowProgress` falls back to seeded `currentCount` when live computation returns 0 (sparse seed trips — TODO flagged for real backend). **Verification:** HTTP 200 across `/`, `/incentives`, `/requests`; `Window: May 6 – May 12` chip renders on Peak Performer + Quick Wins cards (today is 2026-05-12); total-mode cards have NO chip; mode-aware "Current 7-day window" caption replaces "X done · Y to go" for rolling-window mode; window slides daily as device clock advances. **Combined cross-prototype queue: 25 ✅ + 8 remaining** (Manager: P-12, P-13a, P-13b, I-7.5 NEW, I-7 = 5; App: App-I-5, App-I-6, App-I-7 = 3). **🔜 NEXT: paste App-I-5 (Dynamic Ends-in indicator on `<IncentiveCard>`; W2-1; independent — no Manager pair; Auto model).** Pre-paste sandbox cp sync to `Driver App/References/v0-repo-files/` pending in this sync pass.
- **2026-05-12 — Manager P-11 + P-11.1 ✅ Approved LOCAL ONLY — cross-prototype lockstep with App-I-4 ✅ complete (same-day pairing).** Breadcrumb only (Manager TRACKER owns the full entry). Schema bump `2026-05-07-v5 → 2026-05-12-v6` Manager-side. **App-I-4 spec extended** with two additions per user direction: (1) **🆕 Explicit rolling-window date display** — App's `<IncentiveCard>` must render the current applicable rolling-window date range as an EXPLICIT chip (more prominent than Manager preview's muted caption). Port `formatRollingWindow` helper from Manager's `lib/data/incentives.ts` into App's `lib/data/incentive-utils.ts` (or equivalent); render chip when `goal.type === "rolling-window"` AND `today >= startDate`; chip value slides daily as device clock advances; null cases (total mode + upcoming) → no chip. (2) **V1 predicate simplification note** — OnTimePickup `windowDays` may remain informational at the predicate layer if Driver schema only carries 30-day aggregate; Sendbacks `penalty` filter same pattern. **NEW Build Queue entry I-7.5 (Cross-Prototype Component Unification — Bun workspace; driver-facing only; nice-to-have)** queued after App-I-7. Combined cross-prototype queue: **24 ✅ + 9 remaining** (Manager: P-12, P-13a, P-13b, I-7.5, I-7 = 5; App: App-I-4, App-I-5, App-I-6, App-I-7 = 4). **🔜 Next paste: App-I-4 (cross-prototype lockstep paired with P-11) on App working dir `v1/Incentives V1 App/` at :3001 — Higher-thinking model.**
- **2026-05-12 — App-I-3 Empty Market/Client Scope Read-Side Confirmation ✅ Approved LOCAL ONLY.** Cross-prototype lockstep pair for Manager P-10 (which shipped 2026-05-11). **Audit outcome: PASS — no behavior change required.** Codebase grep across `v1/Incentives V1 App/` for `marketScope|clientScope|driver.market|driver.client` returned zero hits outside the seed data file. `lib/data/incentive-utils.ts::getAllIncentiveProgress()` (lines 141–154) maps every enabled `incentiveDefinitions` entry, sorts by `sortOrder` ASC, prioritizes incomplete-first — no scope filter. `CurrentDriver` type (slimmed in I-0) carries `{id, displayName, initials, username}` only; no `market` or `client` field exists to filter against. UI copy grep for "no markets" / "no clients" / "All Markets" / "All Clients" → zero hits (all market/client UI text lives Manager-side). **Code change:** 1 file edited — `lib/data/incentives.ts` lines ~69–78. Added defensive block comments on `IncentiveDefinition.marketScope` + `clientScope`: (1) "Empty array = ALL markets eligible (matches Manager P-10 read semantics, 2026-05-12)"; (2) "App does NOT filter on this field today: `CurrentDriver` carries no `market` field and the dashboard renders every enabled incentive regardless of scope. This is a pure passthrough read"; (3) "If filter logic is ever added (App-I-4 / future), use the length-zero shortcut: `marketScope.length === 0 || marketScope.includes(driver.market)`". Zero behavior change. **Verification:** Manager :3000 → HTTP 307 (root redirect, normal); App :3001 → HTTP 200 (serving cleanly); spot-check App dashboard renders all 8 enabled incentives identically to pre-change. **PRD flag for v1:** Market/Client scope is admin-side analytics, NOT driver-side gating — driver visibility unaffected by these fields in v1. Manager interprets empty=All in UI labels + form validation + scope chips; App passively honors it by not filtering. Different mechanisms, same observable outcome. **Cross-prototype lockstep with Manager P-10 ✅ complete.** Pre-paste sandbox sync N/A (App-I-3 doesn't ship to v0; Path C local-first; v0 backfill pending). Build queue: **23 ✅ + 9 remaining** (Manager: P-11, P-12, P-13a, P-13b, I-7 = 5; App: App-I-4, App-I-5, App-I-6, App-I-7 = 4). Git commits: pending on `v1/Incentives V1 App/` working dir. 🔜 NEXT: paste **Manager P-11** (Schema v6 + goal modes + catalog trim — Higher-thinking model; heaviest prompt of the wave) on Manager working dir `v1/Incentives V1 CS Tool/` at :3000. After P-11 ✅, paste App-I-4 in cross-prototype lockstep.
- **2026-05-09 — RESUME WAVE PLANNED ✅ Phase B sign-off (strategy-impacting — App stream pairs with Manager prompts; v6 schema reads).** Driver App's standalone I-2 Polish formally absorbed into Resume Wave App-I-3..App-I-7. **5 new App prompts queued:** App-I-3 (empty-scope read-side confirm; pairs with Manager P-10) · App-I-4 (v6 schema sync — sliding-window goal progress + new param reads; pairs with Manager P-11) · App-I-5 (Dynamic Ends-in indicator; independent) · App-I-6 (Per-incentive history Counted⏐Missed Out + Disqualified-trip UI; pairs with Manager P-12 disqualification schema) · App-I-7 (Dispute form + Appeal feedback dialogs + re-entry pop-up; pairs with Manager P-13b). **Reads v6 schema:** new goal discriminated union (`{type:"total"|"rolling-window"}`); new OnTimePickup window param; new Sendbacks penalty/range/window param; trimmed catalog (Driver Targeting now 6 entries: Trips Completed · Days Since Last Activity · On-Time Pickup · Driver Address County · Tenure · Sendbacks); new Disqualification + Appeal entities with storage keys `wingz-incentives:appeals:v6` + `wingz-incentives:disqualifications:v6`. **Cross-prototype lockstep:** schema-touching pairs ship Manager-then-App; App verifies on :3001 in lockstep with Manager :3000; paired plan-sync-enforcer per pair. Phase B plan locked at `Project - Driver Incentives/v1/References/Resume Wave - Sequenced Plan.md`. Phase A intake at `Project - Driver Incentives/v1/References/Update Intake - 2026-05-09.md`.
- **2026-05-04 — Prompt 0 (Re-orientation) ✅ Approved.** v0 confirmed v1 scope and surfaced 3 ambiguity questions (mock-trips.ts handling, registry/showcase shell, non-driver component trees). All resolved before I-0 paste.
- **2026-05-04 — I-0 Mega Strip ✅ Approved.** Initial pass deleted variant toggle infra, tier system, leaderboard, `/payout` page, `Trip.revenueAddons`, and downgraded the Earned popup to single-CTA Dismiss. UpcomingPayoutWidget initially downgraded to display-only per original spec.
- **2026-05-04 (later) — I-0 patch (UpcomingPayoutWidget full delete) ✅ Approved.** User revised scope: the entire payout surface (widget + page) defers to v2. Patch deleted the component file and removed the dashboard mount. Dashboard top-of-page is now the "This Month" earnings card. User confirmed: "the prototype looks good."
- **2026-05-05 — I-1 Schema migration ✅ Approved.** All three coupled changes landed: (a) per-incentive admin fields added to `IncentiveDefinition` (color/timeframe/enabled/sortOrder/marketScope/clientScope/trigger; `bonusAmount` promoted to sole $ source); (b) `Trip.incentiveType: IncentiveType | null` → `Trip.incentiveTypes: IncentiveType[]` (multi-incentive trips with stacked pills on the Pill Row Bottom variant); (c) binary progress UI (drop `scheduledCount` / "+N taken" — show only `currentCount` done vs to-go). Re-seeded 8 incentives + ~15 trips with ≥4 multi-incentive examples. `targetCount` → `goal` and `name` → `title` renames complete. `periodId` dropped. Schema version comment `// schemaVersion: 2026-05-04-v2` in place.

## Component Inventory — Post-v1 Strip (target state)

After I-0 → I-2 complete, the v0 repo should contain only these driver-incentive components:

**KEEP (post-strip):**
- `components/driver/header.tsx`
- `components/driver/bottom-nav.tsx`
- `components/driver/ride-card.tsx`
- `components/driver/ride-detail-layout.tsx`
- `components/driver/incentive-pill-renderer.tsx` (formerly `incentive-badge-renderer.tsx`; renamed during I-0 — only `pill-named-bottom` path remains)
- `components/driver/program-contribution-indicator.tsx`
- `components/driver/dashboard-incentive-section.tsx` (only `dashboard-card-section` carousel render path)
- ~~`components/driver/upcoming-payout-widget.tsx` (display-only)~~ — DELETED (revised 2026-05-04 later; entire payout surface defers to v2)
- `components/driver/filter-requests-modal.tsx` (with `incentiveType` field)
- `components/driver/incentive-earned-popup.tsx` (single-CTA Dismiss)
- `components/driver/revenue-display.tsx` (kept; `revenueAddons` stripped from data)

**DELETE (during I-0 strip):**
- `components/driver/variant-toggle.tsx`
- `components/driver/variants-wrapper.tsx`
- `components/driver/tier-badge.tsx`
- `components/driver/tier-progress-section.tsx`
- `components/driver/leaderboard-tab.tsx`
- `components/driver/upcoming-payout-widget.tsx` (revised 2026-05-04 later — full delete, not display-only)
- `components/driver/tier-up-popup.tsx`
- `lib/variants.ts`
- `lib/variants-context.tsx`
- `lib/incentive-sort.ts` (gold-first tier-based sort; replaced by `sortOrder` ASC in I-1)
- `app/payout/page.tsx` + the entire `app/payout/` directory
- Any `/payout`-only helper components (PayoutSummary, PeriodSelector wrapper)

## Step Specs

### Step 0: Re-orientation

**Goal:** Re-orient v0 onto v1 scope. No code changes.

**Inputs:**
- Updated `PROTOTYPE-BIBLE.md` (v1 — describes the END STATE)
- This `PROTOTYPE-TRACKER.md` (v1 — strip-based build queue)
- Reference: `v1 Scope Lock.md` + `CEO Feedback - 2026-05-02 - v1 v2 v3 split.md`

**Acceptance:**
- [ ] v0 confirms it has read both files and understands the v1 scope (variants locked, tier/leaderboard/payout out, per-incentive `bonusAmount`)
- [ ] v0 lists the components/files it expects to delete in I-0 and what's preserved
- [ ] No code changes generated in this step

### Step I-0: Mega Strip

**Goal:** One forward-only deletion pass that removes every v2/v3 surface in a single coherent change. After this step, the prototype is structurally v1 — only schema migration (I-1) and polish (I-2) remain.

**Files to delete:**
- `lib/variants.ts`
- `lib/variants-context.tsx`
- `components/driver/variant-toggle.tsx`
- `components/driver/variants-wrapper.tsx`
- `components/driver/tier-badge.tsx`
- `components/driver/tier-progress-section.tsx`
- `components/driver/leaderboard-tab.tsx`
- `components/driver/tier-up-popup.tsx`
- `lib/incentive-sort.ts` (gold-first tier-based sort — replaced by `sortOrder` ASC in I-1; keep helpers + sort callsites in IncentivesPage / DashboardIncentiveSection but stub the sort with a placeholder ASC-by-id sort that I-1 finalizes)
- `app/payout/page.tsx` + the entire `app/payout/` directory
- Any helper components that exclusively serve `/payout`

**Variant strip:**
- Remove every `useVariants()` import + call. Hardcode the locked variant value at each callsite.
- Remove the floating "Variants" pill button from layout (`app/layout.tsx` or wherever).
- In `IncentiveBadgeRenderer`: keep ONLY the `pill-named-bottom` render path. Delete the `banner-wingz-hero` and `achievement-banner` paths. Rename file/component to `incentive-pill-renderer.tsx` / `IncentivePillRenderer`.
- In `DashboardIncentiveSection`: keep ONLY the `dashboard-card-section` carousel path. Delete `dashboard-banner` and `dashboard-widget-integrated` paths.
- Strip URL query param parsing for variant overrides (`v_pill`, `v_dash`, `payoutSummary`, `tierProgress`, `leaderboard`).
- Strip every `localStorage.getItem('driver-incentives-variants')` and corresponding setItem.
- In `IncentiveCard`: strip the variant-driven theming switch (white/black/tier-color). Card body is always white. Per-incentive color theming will reattach in I-1 via `incentive.color`.

**Tier + Leaderboard strip — schema (`lib/data/incentives.ts`):**
- Delete `Tier` type (`'bronze' | 'silver' | 'gold' | 'platinum'`)
- Delete `IncentiveTierLevel` type
- Delete `TierConfig` type + `tierConfigs[]` seed array
- Delete `LeaderboardEntry` type + `leaderboardEntries[]` seed array
- Delete `INCENTIVE_TIER_BONUSES` constant
- Strip `tierLevel` from `IncentiveDefinition` (other fields will be migrated in I-1)
- Strip `currentTier`, `totalBonusesEarnedThisMonth`, `currentRank`, `totalDrivers`, `county` from `CurrentDriver` (KEEP `id`, `displayName`, `initials`, `username`)

**Tier + Leaderboard strip — `/incentives` page (`app/incentives/page.tsx`):**
- Remove the `Tabs` / `TabsList` / `TabsTrigger` / `TabsContent` wrapper. v1 has NO tabs.
- Delete the Leaderboard tab content + Leaderboard tab header + sticky `YourPlacementCard` + sticky Top 3 region
- Delete the sticky `TierProgressSection` from the top of the (former) Incentives tab
- Page becomes: header (back chevron + "Driver Incentives") + scrollable list of `IncentiveCard`s.

**Earned popup downgrade (`incentive-earned-popup.tsx`):**
- Drop the "View Earnings" CTA (would route to `/payout` — being deleted)
- Drop the "View Achievements" CTA (Leaderboard tab is gone)
- Single CTA: "Dismiss"
- Auto-dismiss after 6s remains.

**Earned popup context (`incentive-earned-context.tsx`):**
- Remove the `showTierUp` function and `TierUpTier` type
- Keep only `showEarned(type: IncentiveType)` and `EarnedPopup` mount

**`/payout` strip — schema (`lib/data/incentives.ts`):**
- Delete `PayPeriod` type + `PAY_PERIODS[]` seed array
- Delete `PayoutPeriodSummary` type + `PAYOUT_PERIOD_SUMMARIES[]` seed array
- Delete `revenueAddons?: { label: string; amount: number }[]` from `Trip` type
- Strip `revenueAddons` arrays from any seeded trips

**`/payout` strip — components:**
- DELETE `components/driver/upcoming-payout-widget.tsx` entirely (revised 2026-05-04 later; previously was a display-only downgrade — user revised because the entire payout surface defers to v2).
- Remove the UpcomingPayoutWidget import + mount from the dashboard (`app/page.tsx` or wherever it's rendered). Dashboard top-of-page becomes: Header → "This Month" earnings card with dot pagination → Confirm Trip prompt → Driver Incentives section → New Requests preview → Next Accepted Ride.
- `components/driver/revenue-display.tsx`: strip the `+$<addons>` rendering and tap-popover. Revenue cell renders `$<base>` only.

**Acceptance:**
- [ ] No `useVariants()` references in code; no `lib/variants.ts`
- [ ] No "Compare Variants" floating pill on screen
- [ ] Ride cards show only `pill-named-bottom`; dashboard incentive section is the carousel only
- [ ] No `Tier` / `TierConfig` / `LeaderboardEntry` / `TierBadge` types
- [ ] No tier-related components in `components/driver/`
- [ ] `/incentives` page has no Tabs — single Incentives view
- [ ] Earned popup has only "Dismiss" CTA
- [ ] No `app/payout/` directory; no `PayPeriod` / `PayoutPeriodSummary` types; no `revenueAddons` in code
- [ ] No `components/driver/upcoming-payout-widget.tsx` file; no UpcomingPayoutWidget mount on dashboard; revenue cells show base $ only
- [ ] grep `UpcomingPayoutWidget` / `upcoming-payout-widget` returns 0 code hits
- [ ] grep "useVariants" / "banner-wingz-hero" / "achievement-banner" / "dashboard-banner" / "dashboard-widget-integrated" / "tier-linear" / "tier-stack" / "leaderboard-list" / "leaderboard-podium" / "boxed-tabs" / "edge-to-edge-tabs" / "tier" (case-insensitive) / "leaderboard" (case-insensitive) / "Bronze|Silver|Gold|Platinum" / "INCENTIVE_TIER_BONUSES" / "podium" / "YourPlacement" / "/payout" / "PAYOUT_PERIOD" / "revenueAddons" → all return 0 code hits (matches in comments noting "v2 only" / "v3 only" are OK)

### Step I-1: Schema migration — per-incentive admin fields + re-seed

**Goal:** The only ADD step. Migrate the schema to fully admin-editable per-incentive fields. `bonusAmount` becomes the sole source of truth for incentive $. Two refinements landed alongside (2026-05-04): **multi-incentive trips** (one trip can count toward multiple programs) and **simplified binary progress UI** (done vs to go — drop the "+N taken" middle state).

**Final `IncentiveDefinition` shape (`lib/data/incentives.ts`):**

```ts
export interface IncentiveDefinition {
  id: string;
  type: IncentiveType;
  title: string;                              // RENAME from existing `name`
  description: string;                        // KEEP
  goal: number;                               // RENAME from existing `targetCount`
  bonusAmount: number;                        // KEEP (already exists; sole $ source post-I-0)
  timeframe: 'daily' | 'weekly' | 'monthly' | 'all-time';   // ADD; replaces `periodId: 'may-2026'`
  color: string;                              // ADD; hex, drives pill bg
  enabled: boolean;                           // ADD
  sortOrder: number;                          // ADD; ASC; lower = higher in list
  marketScope: string[];                      // ADD
  clientScope: string[];                      // ADD
  trigger: string;                            // ADD; read-only enum value (eng-managed)
  qualifyingCriteria: string;                 // KEEP
  iconName?: string;                          // KEEP
  // DROP from current schema: `periodId` (replaced by `timeframe`); `tierLevel` (already stripped in I-0)
}
```

**Sibling type — `DriverIncentiveProgress` (Driver App's progress tracker, same file):**

```ts
export interface DriverIncentiveProgress {
  incentiveId: string;                        // KEEP
  currentCount: number;                       // KEEP — DO NOT rename; this is the "done" count consumed by the binary progress bar
  goal: number;                               // RENAME from existing `targetCount`
  isComplete: boolean;                        // KEEP
  bonusEarned: number;                        // KEEP, but compute changes: `bonusEarned = isComplete ? definition.bonusAmount : 0` (was tier-locked via INCENTIVE_TIER_BONUSES)
  lastQualifyingTripId?: string;              // KEEP
  // DROP: `scheduledCount` (the "+N taken" field — UI no longer reads it)
}
```

**Trip schema change — single → multi-incentive:**

```ts
// BEFORE (v3 baseline + v1 pre-refinement):
//   incentiveType: string | null;            // single program per trip

// AFTER (v1 post-2026-05-04):
incentiveTypes: string[];                     // 0..N programs this trip counts toward; [] if none
```

- Pill Row Bottom variant already supports stacked pills — render one pill per `incentiveType` entry, in `IncentiveDefinition.sortOrder` ASC.
- Empty array → no pills (replaces null suppression).
- ProgramContributionIndicator (popover/tooltip) now lists ALL programs the trip contributes to, one row per program with progress + bonus amount.
- Filter modal: "Filter by Incentive" matches if `trip.incentiveTypes.includes(filterValue)`.
- All `Trip.incentiveType` reads → `Trip.incentiveTypes`. Helper `getTripsForIncentiveType` (existing name in code) refactored from `===` to `.includes()`. `tripHasIncentives` migrates from `trip.incentiveType !== null` to `trip.incentiveTypes.length > 0`.

**Progress simplification — binary done-vs-to-go:**

The current v3 baseline UI shows a 3-state progress: solid green (`X done`) + hatched green (`+Y taken`) + gray (`Z to go`). v1 simplifies to **2-state binary**: solid green (`X done`) + gray (rest of bar). Drop the "taken" intermediate state entirely.

- Progress data: only `currentCount: number` is consumed by the bar/text. If a `takenCount` / in-flight field exists in seed data, leave it but stop reading it from the UI (or drop entirely — it's display-only, not load-bearing).
- Bar: `currentCount / goal` solid fill, rest gray. No hatched section.
- Caption text: `"5 done · 3 to go"` (computed: `goal - currentCount` = to go). NO `+N taken` segment.
- Applies to: `IncentiveCard` on Dashboard carousel + `/incentives` page.
- "Earned this month" sticky aggregate (the green dollar above the tier bar in v3) — kept, but the tier bar itself is GONE in v1 (stripped in I-0).

**Re-seed all 8 incentives:**

| title | bonusAmount | goal | timeframe | color | sortOrder | trigger |
|---|---|---|---|---|---|---|
| Weekend Warrior | 50 | 8 | monthly | `#10B981` | 10 | weekend-trip |
| Peak Performer | 50 | 10 | monthly | `#EAB308` | 20 | peak-hours |
| Early Bird | 30 | 8 | monthly | `#06B6D4` | 30 | before-9am |
| White Glove | 50 | 6 | monthly | `#8B5CF6` | 40 | door-to-door |
| Hometown Hero | 30 | 8 | monthly | `#94A3B8` | 50 | in-county |
| Squad Goals | 50 | 6 | monthly | `#EC4899` | 60 | multi-rider |
| Quick Wins | 10 | 10 | monthly | `#3B82F6` | 70 | short-distance |
| Loyalty Streak | 10 | 5 | monthly | `#F59E0B` | 80 | consecutive-days |

All `enabled: true`, all `marketScope: ['Atlanta']`, all `clientScope: ['Verida', 'MTM']` (vary 1–2 to demonstrate scoping; e.g., White Glove → `['Verida']`, Squad Goals → `['MTM']`).

**Trip seed — multi-incentive examples (REQUIRED):**

Re-seed ~15 trips total. At LEAST 4 must be multi-incentive to demonstrate stacking. Suggested combos (driver story + which programs apply):

| Trip | incentiveTypes | Pills rendered (in sortOrder) |
|---|---|---|
| Saturday short door-to-door pickup, before 9am | `['weekend-warrior', 'early-bird', 'white-glove', 'quick-wins']` | 4 pills: Weekend Warrior + Early Bird + White Glove + Quick Wins |
| Tuesday 7am door-to-door, in-county | `['early-bird', 'white-glove', 'hometown-hero']` | 3 pills |
| Friday 6pm peak hour multi-rider | `['peak-performer', 'squad-goals']` | 2 pills |
| Sunday in-county short distance | `['weekend-warrior', 'hometown-hero', 'quick-wins']` | 3 pills |
| Plain weekday midday single trip | `[]` | No pills |
| Plus ~10 more single-incentive trips spread across all 8 programs | various 1-element arrays | 1 pill each |

Each multi-incentive trip should still complete to a single trip record — the trip "counts toward" each listed program (increments `currentCount` for each). Stacking pills must NOT overflow the ride card; if 4+ pills don't fit, allow horizontal scroll within the bottom pill row OR truncate to first 3 + "+N more" chip (whichever the existing layout already supports cleanest — defer to v0's judgment but note both options).

**Renames across codebase:**
- `IncentiveDefinition.name` → `title`
- `IncentiveDefinition.targetCount` → `goal`
- `DriverIncentiveProgress.targetCount` → `goal` (this field is duplicated from definition; rename in both places)
- `IncentiveDefinition.periodId` → REMOVE (replaced by `timeframe`)
- `Trip.incentiveType: IncentiveType | null` → `Trip.incentiveTypes: IncentiveType[]` (singular nullable → array; use `IncentiveType[]` not raw `string[]`)
- `DriverIncentiveProgress.scheduledCount` → REMOVE (no longer consumed by UI)
- Helpers `getIncentiveByType`, `getIncentiveById`, `getProgressForIncentive` keep working (no key changes)
- Helper `getTripsForIncentiveType(type)` migrate from `t.incentiveType === type` to `t.incentiveTypes.includes(type)`
- Helper `tripHasIncentives(trip)` migrate from `trip.incentiveType !== null && trip.clientEnrolledInIncentives` to `trip.incentiveTypes.length > 0 && trip.clientEnrolledInIncentives`
- `Trip.clientEnrolledInIncentives` field stays unchanged

**Component updates:**
- `IncentivePillRenderer` (renamed from IncentiveBadgeRenderer in I-0): pill bg = `incentive.color`. Pill text = `<incentive.title> Trip`. Text color for contrast (white on dark bg, dark on light bg).
- `RideCard` bottom pill row (`components/driver/ride-card.tsx`): maps `trip.incentiveTypes` → renders one pill per entry, sortOrder ASC. The existing `incentiveType={trip.incentiveType!}` props at lines ~119 and ~200 must change to map over `trip.incentiveTypes`.
- Dashboard `IncentiveCarouselCard` (in `components/driver/dashboard-incentive-section.tsx`): title, description, **binary progress bar (`currentCount/goal` solid green, gray for the rest — NO scheduledCount, NO `+N taken`, NO hatched fill)**, caption `"{currentCount} done · {goal - currentCount} to go"`, "Earn $`bonusAmount`". Color used as left-border accent (3–4px) or small color dot.
- In-card progress block in `components/driver/incentive-badge-renderer.tsx` (the sub-renderer around lines ~103–127): same binary simplification — strip the `progress.scheduledCount` reads, the hatched bar segment, and the `+{progress.scheduledCount} taken` span.
- `ProgramContributionIndicator` (`components/driver/program-contribution-indicator.tsx`, currently formats `progress.currentCount / progress.targetCount` for a single program): rebuild to accept `trip` and render ALL programs in `trip.incentiveTypes` — one row per program with `<title> · X done · Y to go · Earn $<bonusAmount>`.
- `FilterRequestsModal` (`components/driver/filter-requests-modal.tsx`): UI stays singular (driver picks ONE incentive to filter by). Match logic in `app/requests/page.tsx` switches from `t.incentiveType === appliedFilters.incentiveType` to `t.incentiveTypes.includes(appliedFilters.incentiveType)`.
- `DashboardIncentiveSection` carousel: sort by `sortOrder` ASC (replaces the I-0 placeholder sort).
- `IncentivesPage` (`app/incentives/page.tsx`, already had Tabs stripped in I-0): sort by `sortOrder` ASC.

**Acceptance:**
- [ ] `IncentiveDefinition` matches the v1 shape above (no `name`, no `targetCount`, no `periodId`, no `tierLevel`)
- [ ] `DriverIncentiveProgress` has `goal` (not `targetCount`), keeps `currentCount`, has NO `scheduledCount`
- [ ] `Trip.incentiveTypes: IncentiveType[]` everywhere (no `Trip.incentiveType` singular nullable)
- [ ] `tripHasIncentives` and `getTripsForIncentiveType` migrated to use the array form
- [ ] No `tierLevel` / `INCENTIVE_TIER_BONUSES` / `targetCount` / `name` / `periodId` / `scheduledCount` references remain in schema or seed
- [ ] All 8 seeded incentives have all v1 fields populated
- [ ] At least 4 seeded trips have ≥2 entries in `incentiveTypes` and render multiple stacked pills
- [ ] Trips with `incentiveTypes: []` render no pills (replaces null suppression)
- [ ] IncentiveCard progress UI shows ONLY `"{currentCount} done · {goal - currentCount} to go"` (binary bar, no `+N taken`, no hatched fill, no `scheduledCount` reads)
- [ ] Pill bg colors visibly vary across incentives in screenshots; multi-pill stacking visible on ≥4 ride cards
- [ ] Programs sorted by `sortOrder` ASC on dashboard + `/incentives`
- [ ] Filtering by an incentive returns trips where that incentive is in `incentiveTypes` (not just trips where it's the sole `incentiveType`)
- [ ] grep `Trip\.incentiveType[^s]` returns 0 hits (singular nullable form fully migrated)
- [ ] grep `scheduledCount` returns 0 hits
- [ ] grep `targetCount` returns 0 hits (replaced by `goal`)
- [ ] grep `taken` (case-insensitive) in driver components returns 0 hits

### Step I-2: Polish + edge states + final QA grep sweep

**Goal:** Land v1. No new features — just edge states + sweep.

**Edge states:**
- Dashboard incentive section, no active incentives in scope: empty card "No active incentives this period. Check back soon."
- `/incentives` page, no active incentives: same message.
- Requests page filtered by an incentive that has no eligible trips: "No incentive-eligible trips right now."
- Disabled incentives (`enabled: false`) MUST NOT render anywhere — filter at the data-helper layer.

**Earned popup:**
- Add a small floating [DEV] button at bottom-right (prototype only) that opens a list of the 8 incentives. Tap any to fire the Earned popup (mock-trigger; no real flow events).
- Auto-dismiss after 6s confirmed.
- Single CTA "Dismiss" (verified from I-0).

**Final QA grep sweep — these MUST return 0 code hits (matches in comments noting "v2 only"/"v3 only" are OK):**
- `tier` (case-insensitive)
- `leaderboard` (case-insensitive)
- `Bronze` / `Silver` / `Gold` / `Platinum`
- `INCENTIVE_TIER_BONUSES`
- `useVariants`
- `banner-wingz-hero` / `achievement-banner`
- `dashboard-banner` / `dashboard-widget-integrated`
- `/payout` (in routes)
- `revenueAddons`
- `targetCount`
- `pointsEarned` / `pointsAccumulated`
- `TierConfig` / `LeaderboardEntry` / `TierBadge`

**Screenshot pass:**
- Dashboard (top → bottom): "This Month" earnings card with dot pagination → Confirm Trip prompt → Driver Incentives carousel → New Requests preview → Next Accepted Ride. NO UpcomingPayoutWidget — it was deleted in I-0 (full payout surface defers to v2).
- `/incentives` (single view, no tabs)
- Requests with `?incentive=weekend-warrior` filter
- Ride card with pill (each of 8 incentives produces a different colored pill)
- Ride detail with pill below trip metadata card
- Earned popup ([DEV] triggered) for at least 3 different incentives
- Filter modal with `Incentive` dropdown row

**Acceptance:**
- [ ] All 4 edge states render correctly
- [ ] [DEV] Earned popup trigger works for each of 8 incentives
- [ ] Disabled incentives don't render
- [ ] All grep checks return 0 hits
- [ ] Visual sweep against v1 BIBLE — nothing tier/leaderboard/payout/variant-toggle visible
- [ ] If everything passes, mark v1 Driver App ✅ Approved in this TRACKER

> **Note (post-Resume Wave Phase B 2026-05-09):** Standalone I-2 Polish absorbed into Resume Wave App-I-3..App-I-7 (no longer ships as a discrete prompt). Edge state polish + grep sweep concerns redistributed into App-I-3 (empty-scope confirm), App-I-5 (ends-in indicator polish), App-I-6 (history view polish), App-I-7 (appeal flows polish), and Manager I-7 (final cross-prototype sweep). Spec retained above for historical reference + grep targets that remain valid.

---

### Step App-I-3: Resume Wave Empty Market/Client Scope Read-Side Confirmation

**Goal:** App-side half of W1-4 (intake item: empty Market/Client array → "All"). Pairs with Manager P-10. Confirms App's eligibility filter reads empty `marketScope` / `clientScope` arrays as "all markets / all clients" rather than "none."

**Tasks:**

1. **Audit current eligibility filter logic.**
   - Find the helper(s) that decide whether an incentive shows for the logged-in driver. Likely in `lib/data/incentives.ts` or a sibling utility.
   - Common pattern: `incentive.marketScope.length === 0 || incentive.marketScope.includes(driver.market)`. If this pattern is present, empty-as-all already works → confirm via test, no code change.
   - Anti-pattern: `incentive.marketScope.includes(driver.market)` (without length check). If this pattern is present, empty arrays incorrectly filter the incentive out → fix by adding length-zero shortcut.

2. **Same audit for `clientScope`** (and if Driver Targeting Market/Client variants from P-7a still exist post-P-11 catalog trim — they're being dropped in P-11, so this is a temporary check).

3. **Update copy in App** (if any) that references "no markets" / "no clients" for empty arrays. Likely none in App side (UI text tends to live in Manager); but spot-check incentive detail / popovers.

**Acceptance:**
- [x] Eligibility filter handles empty `marketScope` as "all markets eligible." → ✅ Trivially: no filter exists; pure passthrough.
- [x] Eligibility filter handles empty `clientScope` as "all clients eligible." → ✅ Same as above.
- [x] Manual test: create a Manager incentive with empty Market + Client → driver in any market sees the incentive on dashboard. → ✅ Driver sees every enabled incentive regardless of scope today.
- [x] No regressions on existing incentives with non-empty scopes. → ✅ Comment-only change; behavior identical.

**Status:** ✅ Approved 2026-05-12 LOCAL ONLY (audit-only, comment-only change). v0 backfill pending.

**Pairs with:** Manager P-10 ✅ (lockstep complete). Auto model.

---

### Step App-I-4: Resume Wave v6 Schema Sync — Goal Modes + Targeting Param Extensions + Explicit Rolling-Window Date Display

**Goal:** App-side companion to Manager P-11 + P-11.1 (the v6 schema bump). Reads new `goal` discriminated union + handles sliding-window progress logic + reads extended OnTimePickup / Sendbacks param shapes + trimmed catalog. **NEW per user direction 2026-05-12 (P-11.1 hook):** render the current applicable rolling-window date range EXPLICITLY on the App's IncentiveCard — more prominent than the Manager preview's subtle caption.

**Tasks:**

1. **Goal discriminated union read (W1-2).**
   - `incentive.goal` shape: `{ type: "total"; count: number } | { type: "rolling-window"; count: number; days: number }`.
   - For `type === "total"`: existing `currentCount / count` rendering preserved. Caption: "X done · Y to go."
   - For `type === "rolling-window"`: NEW sliding-window logic. Track `qualifyingTrips: Array<{date: Date}>` per driver+incentive. Compute "best window so far" by scanning all valid Y-day windows in `[startDate, today]` and finding the max trip count.
     - Mock implementation: precompute a list of trip dates from seed data; iterate over each trip date as a window start; count trips in `[start, start+Y days]`; return max.
     - Caption: "**Current** <Y>-day window: <done> done · <remaining> needed" (corrected 2026-05-12 per user direction — driver-facing metric is "what's my count in the current window right now?", not "best ever achieved").
     - Helper: `computeCurrentWindowProgress(type, count, days, startDate, endDate, today): { done: number; remaining: number }` — counts trips in `[today - (days-1), today]`, clamped to startDate.
     - Future enhancement (deferred from V1): on completion, freeze the chip to the window where the driver completed (would require a stored `completedWindowFromIso`/`ToIso` field on `DriverIncentiveProgress`). V1 uses the existing "Completed" badge + opacity treatment as the completion indicator.

2. **OnTimePickup param read extension (W1-3).**
   - New shape: `{ thresholdPct: number; windowDays: number }`.
   - App's predicate matching against driver state: `driver.otpPercentLast<windowDays>Days >= thresholdPct`.
   - If `driver` doesn't carry pre-computed window-specific OTP, compute from `driver.otpHistoryLast30Days` or fall back to existing `driver.otpPercent` field.
   - **Note (V1 simplification carried from Manager P-11):** if Driver schema only carries 30-day aggregate, `windowDays` may remain informational; predicate reduces to `driver.otpPercent >= thresholdPct`.

3. **Sendbacks param read extension (W1-3).**
   - New shape: `{ penalty: 'penalty' | 'no-penalty' | 'both'; minCount?: number; maxCount?: number; windowDays: number }` (Manager P-11 final shape — `countRange` denormalized to flat `minCount`/`maxCount` fields).
   - App's predicate filters driver's sendbacks by `penalty` filter, counts within `windowDays`, checks against `{minCount, maxCount}` range.
   - **Note (V1 simplification carried from Manager P-11):** if Driver schema only carries raw `sendbacksLast30Days: number`, `penalty` filter remains informational.

4. **Catalog trim handling (W1-3).**
   - Trip Targeting catalog locked to 6 entries; Driver Targeting catalog locked to 6 entries (per Manager P-11).
   - App's predicate evaluators (if any — most predicate logic stays Manager-side; App reads results) tolerate the smaller catalog. Drop any reads of removed types.

5. **🆕 Explicit rolling-window date display (P-11.1 hook from user direction 2026-05-12).**
   - **Port `formatRollingWindow` helper from Manager** into App's `lib/data/incentive-utils.ts` (or equivalent — keep semantic identical so cross-prototype reads stay locked). Manager source: `v1/Incentives V1 CS Tool/lib/data/incentives.ts::formatRollingWindow(goal, startDate, endDate, today)`. Returns `{fromIso, toIso, fromLabel, toLabel} | null`. Pointer clamps to `endDate`; start clamps to `startDate`.
   - **Render on the App's `<IncentiveCard>` (dashboard carousel tile):** when `goal.type === "rolling-window"` AND `today >= startDate`, render an EXPLICIT chip / prominent row displaying the current window: `[Window: May 6 – May 12]` or `[📅 May 6 – May 12 (this week's window)]`. Style suggestion: small bordered chip with leading calendar icon directly under the progress bar — visually distinct from Manager preview's muted caption. Phase D builder picks final treatment based on existing card real estate.
   - **Slides daily:** caption value driven by `today: Date = new Date()`. As days pass on the device clock the displayed range advances automatically.
   - **Same `null` cases as Manager:** total-mode + upcoming campaigns → no chip rendered.
   - **Coordinates with `<AppealResultDialog>` (App-I-7)** — the helper signature should be portable to the shared package created in I-7.5 without changes.

6. **Plan B (if sliding-window logic too complex):** ship "total" mode only + the explicit rolling-window date display (task 5 — UI-only); gate sliding-window progress computation behind a feature flag. Phase D builder picks based on time pressure.

**Acceptance:**
- [x] Total-mode incentives render correctly. → ✅ existing "X done · Y to go" caption preserved for 6 total-mode incentives.
- [x] Rolling-window mode incentives render correctly with sliding-window progress caption. → ✅ "Current 7-day window: X done · N needed" (NOT "Best window" — corrected per user direction).
- [x] **🆕 Rolling-window incentive cards render an EXPLICIT date-range chip showing the current applicable window (e.g., "Window: May 6 – May 12"). Total-mode cards do NOT render this chip.** → ✅ bordered green chip with `CalendarRange` icon rendered on Peak Performer + Quick Wins; suppressed on total-mode cards.
- [x] **🆕 Window value updates as the device clock advances day-to-day (no stale dates).** → ✅ helper signature accepts `today: Date = new Date()` parameter; read at render time.
- [x] **🆕 Treatment is visually MORE prominent than Manager preview (e.g., bordered chip vs. muted caption per user direction).** → ✅ bordered green chip + leading icon vs Manager's `text-[8px] text-muted-foreground` caption.
- [x] OnTimePickup eligibility check handles new threshold + window params. → ✅ shape extended; V1 simplification at predicate layer doc'd (informational `windowDays`).
- [x] Sendbacks eligibility check handles new penalty + range + window params. → ✅ shape extended; V1 simplification at predicate layer doc'd (informational `penalty`).
- [x] No reads of removed catalog types crash. → ✅ App doesn't model catalogs; `IncentiveDefinition` carries `qualifyingCriteria` string only (App-only subset).
- [x] Schema version reads cleanly as `2026-05-12-v6`. → ✅ comment at top of `lib/data/incentives.ts` + PROTOTYPE-BIBLE schemaVersion line.
- [x] **🆕 Semantic correction landed:** "Best Y-day window" → "Current Y-day window" per user direction (driver-facing metric; window slides forward each day; completion lock via existing `isComplete` badge).
- [x] **🆕 Pre-existing bug fixes:** `progress.targetCount` → `progress.goal` in popover + `data.def.name` → `data.def.title` in Earned popup (both stale refs left over from I-1 grep sweep).

**Status:** ✅ Approved 2026-05-12 LOCAL ONLY. v0 backfill pending. **Pairs with:** Manager P-11 + P-11.1 ✅ (lockstep complete). Higher-thinking model.

**Pairs with:** Manager P-11. Higher-thinking model. Ships immediately after P-11 in cross-prototype lockstep.

---

### Step App-I-5: Resume Wave Dynamic Ends-in Indicator on Incentive Card

**Goal:** Add an end-of-campaign indicator to the dashboard `<IncentiveCard>` (the carousel tile). From W2-1.

**Tasks:**

1. **New helper `formatEndsIn(endDate, today): { copy: string; tone: 'urgent' | 'neutral' }`.**
   - `daysUntilEnd = Math.ceil((endDate - today) / day)`.
   - If `daysUntilEnd <= 0`: copy = "Ended" (gray, end-of-campaign state — incentive shouldn't show but defensive).
   - Else if `daysUntilEnd <= 7`: copy = `"Ends in <N> day${N!==1?'s':''}"`, tone = `'urgent'`.
   - Else: copy = `"Ends <Mon DD>"` (e.g., "Ends Mar 20"), tone = `'neutral'`.

2. **Rendering on `<IncentiveCard>`.**
   - Slot: top-right of the card OR small chip below the bonus pill — Phase D builder picks based on layout.
   - Tone styling:
     - `urgent`: amber (e.g., `bg-amber-100 text-amber-800` or `text-amber-700` text-only).
     - `neutral`: muted (`text-muted-foreground` text-only or subtle gray chip).
   - Reads `incentive.endDate` (already wired post-P-6 v5; no schema change).

3. **Application:** dashboard incentive carousel tile + `/incentives` page tile (same component). NOT on ride card pills.

**Acceptance:**
- [ ] All active incentive cards on dashboard show ends-in indicator.
- [ ] When `daysUntilEnd <= 7`: amber + relative copy ("Ends in 3 days").
- [ ] When `daysUntilEnd > 7`: neutral + absolute copy ("Ends Mar 20").
- [ ] Existing card layout doesn't regress.
- [ ] Helper is unit-testable in isolation.

**Independent — no Manager pair.** Auto model. Can ship anytime after App-I-4 (uses v6 schema reads but doesn't need new fields beyond `endDate`).

---

### Step App-I-6: Resume Wave Per-Incentive History (Counted ⏐ Missed Out tabs) + Disqualified-Trip UI

**Goal:** Merged feature combining W2-2 (tap incentive → see completed qualifying rides) + W3-A1 (disqualified-trips view) + W3-A2 (disqualified-trip unique UI). Reads new Disqualification + Appeal collections from Manager P-12.

**Tasks:**

1. **Tap target on dashboard `<IncentiveCard>`.**
   - New chevron / expand affordance opens a per-incentive history view.
   - Disambiguation: existing tap-to-filter Requests behavior preserved on the card body / pill / progress bar. New tap target is dedicated chevron OR "View history" link OR sub-section of card. Phase D locks based on layout.

2. **Per-incentive history view component / route.**
   - Option A: new route `app/incentives/[id]/history/page.tsx`.
   - Option B: inline-expand below the dashboard card.
   - Phase D builder picks based on user's v3 reference code shape (user will paste v3 code at App-I-6 paste time per intake note).
   - Header: incentive title + bonus + status (Active / Ended).

3. **Tab strip "Counted ⏐ Missed Out (N)" where N = pending+resolved disqualifications for this driver+incentive.**

4. **Counted tab.**
   - Simple list (NOT full RideCard chrome — lighter format per user direction).
   - Each row: Trip date · Pickup → Dropoff (city or county) · Bonus contribution.
   - Reuses v3 simple-list pattern (user will paste v3 reference code at App-I-6 paste time).

5. **Missed Out tab.**
   - List of trips that triggered disqualification (read from `disqualifications` collection filtered by driverId + incentiveId).
   - Each row rendered with **disqualified-trip unique UI:**
     - Desaturated card (lighter contrast, gray-ish bg).
     - Amber/red "Disqualified" badge (color: amber to leave red for true errors).
     - Brief reason line: "OTP dropped to 92% — required 95%" or "Sendback hit max (3) in 30 days."
     - Tap action: opens dispute form (App-I-7) IF status === 'pending' or no appeal exists yet; shows "Appeal under review" badge + read-only summary if appeal pending; shows resolved state if appeal approved/denied.

6. **Helpers needed:**
   - `getDisqualificationsFor(driverId, incentiveId)` — filter from collection.
   - `getAppealForDisqualification(disqualificationId)` — find appeal if exists.
   - `formatDisqualificationReason(disqualification)` — render reason copy from `failedRule + computedValues`.

**Acceptance:**
- [ ] Tap target on incentive card opens history view.
- [ ] Tab strip switches between Counted / Missed Out cleanly.
- [ ] Counted tab shows simple list of qualifying trips.
- [ ] Missed Out tab shows disqualified trips with desaturated UI + amber badge + reason.
- [ ] Pending appeals show "Appeal under review" state.
- [ ] Approved/denied appeals show resolved state.
- [ ] Existing tap-to-filter-Requests behavior on incentive card still works.

**Pairs with:** Manager P-12 (App reads disqualifications + appeals seeded by P-12). Higher-thinking model.

---

### Step App-I-6.1: NEW 2026-05-12 (post-compact ideation) — Per-Incentive Rides View + Always-On Criteria List + 4-State Appeal Button

**Goal:** Reframe App-I-6 per the post-compact ideation locked design. Drop the `Counted ⏐ Missed Out` tab strip; rename route + view; add always-on Eligibility criteria block iterating `incentive.driverTargeting` with ✓/✗ states; surface a 4-state appeal button per ✗ card; keep rides list unconditional below the criteria block.

**Lock first:**
- Eligibility criteria are evaluated against `incentive.driverTargeting` rules. Each rule that has an open `Disqualification` (without an approved appeal) renders as ✗; otherwise ✓.
- Approved appeal flips the criterion to ✓ (exempted) — the `"Appeal won"` button stays as a paper trail.
- Disqualified banner = at least one ✗ in the criteria list. Banner hides when all cards resolve to ✓.
- Appealability is **per missed criterion**, not pre-classified by rule type.
- Goal progress is independent of eligibility — rides list unconditional. Earning the incentive at campaign end requires goal reached AND zero ✗ criteria.

**Tasks:**

1. **Route + CTA rename.**
   - `app/incentives/[id]/history/page.tsx` → `app/incentives/[id]/rides/page.tsx`.
   - `components/driver/dashboard-incentive-section.tsx`: `"View history →"` → `"View rides →"`; Link target → `/rides`. Keep existing icon (or swap `History` → `ListChecks` lucide).
   - Rename `components/driver/incentive-history-view.tsx` → `components/driver/incentive-rides-view.tsx`. Update default export name → `IncentiveRidesView`.

2. **Remove tabs.** Delete `Counted | Missed Out` `<TabButton>` group + `activeTab` state + `buildMissedOutRows` helper. Header card unchanged from App-I-6.

3. **Eligibility criteria block (NEW, always renders if `incentive.driverTargeting.length > 0`).**
   - Heading: `"Eligibility criteria"`.
   - Iterate `incentive.driverTargeting`; render `<CriterionCard>` per rule.
   - Card composition: rule label via `formatCriterionLabel(rule)` + threshold detail line via `formatCriterionDetailLine(rule, computedValues?)` + ✓/✗ state badge + appeal button slot (conditional per state matrix).
   - Above the block: if any ✗ card → `<DisqualifiedBanner>` (amber-50 bg + amber-200 border + `AlertTriangle` icon + copy `"Temporarily disqualified — appeal a criterion below to request a review. Trips continue to count toward your goal."`)

4. **`<CriterionCard>` state matrix** (derived by `getCriterionState`):

| Failing? | Appeal record? | Card state | Button |
|---|---|---|---|
| No | n/a | ✓ | none |
| Yes | no appeal | ✗ | `"Appeal"` primary → placeholder `alert("App-I-7 will wire <DisputeAppealSheet mode='create'> here")` |
| Yes | `pending` | ✗ | `"Appeal pending"` muted pill → placeholder `alert("App-I-7 will wire <DisputeAppealSheet mode='view-pending'> here")` |
| Yes | `approved` ("won") | ✓ exempted | `"Appeal won"` green outline → opens `<AppealResultDialog>` with manager note |
| Yes | `denied` ("lost") | ✗ | `"Appeal lost"` red outline → opens `<AppealResultDialog>` with manager reason |

   - `<AppealResultDialog>` minimal impl in App-I-6.1 (App-I-7 refines + adds sheet modes). Same dialog regardless of won/lost; tone + copy switch on appeal status. Modal wrapper (shadcn AlertDialog or Dialog) + green check / red X icon + manager note/reason text + Dismiss button.

5. **Rides list section** below the criteria block.
   - Heading: `"Your rides"`.
   - Reuse `buildCountedTripRows` unconditionally. Drop "Restored" badge from rows (criterion card carries appeal state instead).
   - Empty state: `"No qualifying trips yet. Eligible trips will appear here as you complete them."`

6. **Helpers (NEW).**
   - `getCriterionState(rule, disqualifications, appeals, driverId, incentiveId): { state: '✓' | '✗', appealStatus: 'none' | 'pending' | 'approved' | 'denied', disqualification?: Disqualification, appeal?: Appeal }`.
   - `formatCriterionLabel(rule): string` — rule-type-aware ("On-Time Pickup ≥ 95% over 7 days", "Sendbacks ≤ 2 per 14 days", etc.).
   - `formatCriterionDetailLine(rule, computedValues?): string` — `"Currently 92% · Required 95%"` when failing; `"Currently meeting"` when ✓.
   - Delete `buildMissedOutRows`.

7. **Seed coverage.** Ensure `currentDriver.id` ('driver-7821') has at least: 1 incentive with no-appeal ✗, 1 with pending ✗, 1 with approved ✓-exempted, 1 with denied ✗, plus all-✓ incentives elsewhere. Reuse existing P-12-mirrored seeds; adjust appeal statuses if needed.

**Acceptance:**
- [ ] Route `/incentives/[id]/rides` renders the new view; `/history` route gone.
- [ ] Dashboard CTA reads `"View rides →"` and links to `/rides`.
- [ ] Header card unchanged from App-I-6.
- [ ] Eligible driver (all ✓): criteria block shows all ✓ cards, no banner, no appeal buttons.
- [ ] Mixed eligibility: ✗ cards show appropriate appeal button per state matrix; ✓ cards show no button; banner present.
- [ ] All 4 appeal states render correctly + open the right interaction.
- [ ] Approved appeal flips card to ✓ + retains "Appeal won" button.
- [ ] Banner hides when no ✗ remain after approvals.
- [ ] Rides list unconditional; no decoration on approved-appeal trips.
- [ ] `<AppealResultDialog>` shows manager note/reason on tap of "Won"/"Lost" buttons.
- [ ] No schema change.

**Pairs with:** none directly (pure App refactor on top of App-I-6 commit `10a8692`); tightens App-I-7 scope. Higher-thinking model.

---

### Step App-I-6.2: NEW 2026-05-12 (post-compact ideation) — Driver-Level Past Incentives Archive (Past segment on `/incentives` page)

**Goal:** Add a driver-level past incentives archive on the existing `/incentives` page via an Active | Past segment toggle. Lists ended incentives the driver participated in, each with an outcome badge (Earned +$X / Missed — goal not reached / Missed — appeal lost). Tap row → opens `/incentives/[id]/rides` in its naturally-frozen end state (criteria block + rides list in final state; no "Appeal" primary CTAs).

**Tasks:**

1. **Segment toggle on `/incentives` page.**
   - Add `Active | Past` toggle at the top of the page (above the existing list). Use shadcn Tabs or a simple segmented control (`<button>` group with active state styling).
   - Default tab = `Active`. State persists in URL query param `?tab=past` for shareability (optional v1.1; v1 can stash in component state).
   - Active tab = existing list (current + upcoming incentives). No changes.
   - Past tab = new list driven by `seedPastOutcomes` + ended incentives.

2. **Past tab content.**
   - List of `<PastIncentiveRow>` components — one per ended incentive the driver has a `seedPastOutcomes` row for.
   - Row composition (left → right):
     - Color chip (incentive color)
     - Incentive title + date range subtitle ("May 1 – May 14")
     - Outcome badge (right):
       - `Earned +$50` — emerald-50 bg + emerald-200 border + emerald-700 text + `CheckCircle2` lucide icon
       - `Missed — goal not reached` — gray-100 bg + gray-300 border + gray-700 text
       - `Missed — appeal lost` — red-50 bg + red-200 border + red-700 text + `XCircle` lucide icon
     - Chevron right indicator
   - Tap row → `router.push('/incentives/[id]/rides')`.
   - Empty state (no past outcomes): `"No past incentives yet. Completed and missed-out incentives will appear here after they end."`

3. **Seed data — `seedPastOutcomes` table.**
   - New seed array in `lib/data/incentives.ts` (or a new `lib/data/past-outcomes.ts` if cleaner):
   ```ts
   export interface PastOutcome {
     incentiveId: string;
     driverId: string;
     outcome: 'earned' | 'missed-goal' | 'missed-criterion';
     amountEarned?: number;
     missedReason?: string;
   }
   export const seedPastOutcomes: PastOutcome[] = [
     // 3–5 entries for currentDriver.id ('driver-7821') referencing ended incentives
   ];
   ```
   - Add 3–5 ended incentives to App seed (status `ended` via `deriveStatus` — needs `endDate < today`). E.g., a Peak Performer that ran May 1–7 with outcome `earned`, a Quick Wins that ran April 15–30 with outcome `missed-goal`, a White Glove that ran April 1–15 with outcome `missed-criterion` + reason "OTP appeal denied."
   - Optional: add `migratePastOutcome` deserializer if persisted (not strictly required for v1 since these are seed-only and don't accept mutations).

4. **Helper — `getPastOutcomesFor(driverId): { incentive: IncentiveDefinition, outcome: PastOutcome }[]`.**
   - Filters `seedPastOutcomes` by `driverId` + joins each row to its `IncentiveDefinition` from `seedIncentives`.
   - Returns sorted by incentive `endDate` desc (most recent first).
   - Filters out any rows whose incentive doesn't exist anymore (defensive).

5. **Render behavior on ended incentives at `/rides` route.**
   - When `deriveStatus(incentive) === 'ended'`:
     - Status pill in header reads "Ended" with gray tone.
     - Criteria block still renders (frozen final ✓/✗ states + won/lost buttons as paper trail).
     - **NO "Appeal" primary CTA** appears anywhere (campaign is over). Pending-pill, Won-button, Lost-button can still show as records of past actions.
     - Rides list still renders all qualifying trips.
     - Disqualified banner does NOT render on ended incentives (it's prescriptive about action that's no longer possible).
   - Existing `<IncentiveRidesView>` from App-I-6.1 gains a `readOnly` derivation based on `deriveStatus(incentive)`.

**Acceptance:**
- [ ] `/incentives` page renders `Active | Past` toggle at top.
- [ ] Active tab default; existing list unchanged.
- [ ] Past tab renders one row per `seedPastOutcomes` entry for current driver.
- [ ] Outcome badges render correct tone + copy per outcome type.
- [ ] Tap past row navigates to `/incentives/[id]/rides` for the ended incentive.
- [ ] On ended incentive `/rides` page: status pill = "Ended"; no "Appeal" primary CTA appears; criteria block shows final state with won/lost buttons retained; disqualified banner hidden.
- [ ] Empty state renders when driver has no past outcomes.
- [ ] No schema change.

**Pairs with:** none directly (pure App). Depends on App-I-6.1 (`<IncentiveRidesView>` + criteria block rendering). Higher-thinking model.

---

### Step App-I-7: Resume Wave Dispute Form + Appeal Feedback (Approved / Denied + Re-entry Pop-up)

> **🟢 Post-ideation spec corrections (2026-05-12) — read first:** App-I-6.1 reframed the per-incentive view to drop tabs + add an always-on criteria block. App-I-7 is updated accordingly. **Key changes:**
> - `<DisputeAppealSheet>` becomes **mode-aware**: `'create'` (form + submit; existing spec below) and `'view-pending'` (read-only — criterion banner + disabled `<Textarea>` showing submitted text + submitted date + Close button; NO submit). Both share the same bottom-sheet shell.
> - **Pre-filled context banner reads from the criterion** (`failedRule` + `computedValues` on the `Disqualification`), NOT a trip ID. Examples: `"On-Time Pickup at 92% — required 95% over the last 7 days."` / `"3 sendbacks in the last 14 days — limit is 2."` / `"Service-level average at 88% — required 95%."`
> - `<DisputeAppealSheet>` opens from **tapping `"Appeal"` or `"Appeal pending"` buttons on a `<CriterionCard>`** (App-I-6.1), NOT from tapping a disqualified trip row (there are no per-trip disqualified rows anymore).
> - `<AppealResultDialog>` is now invoked from BOTH the dashboard re-entry pop-up (Task 3 below) AND from tapping `"Appeal won"` / `"Appeal lost"` buttons on a `<CriterionCard>` (App-I-6.1 builds a minimal version; App-I-7 refines + adds the re-entry pop-up trigger).
> - Subtitle in sheet header: was `"Disputed Ride"` → now `"Appeal a missed criterion"`.
> - Reason field placeholder: generalized to `"Explain why this criterion shouldn't disqualify you for this incentive..."` (was trip-specific).
> - **Appealability is per missed criterion** (not pre-classified by rule type) — every ✗ criterion card surfaces the appeal CTA regardless of rule type.
> - Acceptance gains: `[ ] Tapping "Appeal pending" pill opens <DisputeAppealSheet mode="view-pending">; banner reads from criterion; textarea is read-only; submitted date visible.` and `[ ] Tapping "Appeal won" or "Appeal lost" buttons on a criterion card opens <AppealResultDialog> with manager's note/reason.`

**Goal:** Bundles W3-A3 (dispute form bottom sheet) + W3-A4 (appeal status feedback dialogs + re-entry pop-up). Reads v6 schema; writes appeals via `createAppeal(...)`; reads appeal status updates from Manager P-13b.

**Tasks:**

1. **`<DisputeAppealSheet>` bottom sheet component.**
   - Pattern mirrors Late Reasons pickup-reason sheet (user-referenced screenshot).
   - Header: trip ID + "Disputed Ride" subtitle + close button (×).
   - **Pre-filled context banner** (red-tinted): the SPECIFIC disqualification reason + values from the `Disqualification` record (e.g., "Pickup at 1:17 PM · 17 mins late" or "OTP dropped to 92% (required: 95%)"). Banner uses red-50 bg + red-900 text + Clock icon.
   - **Reason field:** `<Textarea>` with placeholder "Explain why this trip should still count..." Required, non-empty validation.
   - **Comments field:** optional `<Textarea>` for additional details.
   - Cancel + "Submit Appeal" buttons (sticky bottom).
   - On submit: call `createAppeal({ driverId: currentDriver.id, incentiveId, disqualificationId, driverText: <reason + comments concatenated> })`. New appeal status = `'pending'`. Sheet closes.

2. **Disqualified-trip render mode updates** (extends App-I-6 surface):
   - **No appeal yet:** dispute affordance (CTA "Dispute this trip" or similar).
   - **Pending appeal:** read-only "Appeal under review" badge + driver's appeal text summary.
   - **Approved appeal:** trip re-renders as Counted (moves from Missed Out → Counted on tab switch). Dispute affordance hidden.
   - **Denied appeal:** stays in Missed Out; manager's reason text visible inline; Dispute affordance hidden.

3. **`<AppealResultDialog>` re-entry pop-up.**
   - Triggered on entry to dashboard OR per-incentive history view when there's an unacknowledged resolved appeal.
   - Detection: scan `appeals` collection for current driver where `status !== 'pending'` AND `id NOT IN wingz-incentives:appeal-acks:v1` localStorage.
   - If approved: green check icon + headline "Your appeal was approved" + body "[Incentive Name] is back on track."
   - If denied: amber warning icon + headline "Appeal denied" + body shows `managerReason` text.
   - Single CTA: "Dismiss" (writes `appealId` to ack-state localStorage `wingz-incentives:appeal-acks:v1` array; closes dialog; doesn't re-show for that appeal).
   - If multiple unacknowledged appeals: queue them, show one at a time.

4. **Re-disqualification post-approval** (edge case):
   - If a driver was previously approved on Appeal A, but later disqualified again (different `Disqualification` record), allow new appeal (separate `Appeal` record). UI treats this as a fresh dispute flow.

5. **Touches:**
   - New `<DisputeAppealSheet>` component.
   - New `<AppealResultDialog>` component.
   - Modifications to `<DisqualifiedTripCard>` (or whichever component App-I-6 ships) to handle pending/approved/denied/re-disqualified states.
   - New helper `getUnacknowledgedAppeals(driverId): Appeal[]` reading from collection + localStorage ack-state.
   - New helper for ack-state localStorage read/write.

**Acceptance:**
- [ ] Tap on disqualified trip opens `<DisputeAppealSheet>`.
- [ ] Pre-filled context banner shows disqualification reason + values.
- [ ] Reason validation enforces non-empty.
- [ ] Submit creates new Appeal record with status `pending`.
- [ ] Trip render mode updates correctly across no-appeal / pending / approved / denied states.
- [ ] `<AppealResultDialog>` shows on dashboard entry when there's an unacknowledged resolved appeal.
- [ ] Approved dialog: green check + "back on track" copy.
- [ ] Denied dialog: amber warning + manager reason text visible.
- [ ] Dismiss writes ack to localStorage; dialog doesn't re-show.
- [ ] Re-disqualification path allows fresh appeal.

**Pairs with:** Manager P-13b (App reads appeal mutations performed in Manager). Higher-thinking model.

## Compaction Guard

If you (the v0 agent) lose context:

1. Re-read this file + `PROTOTYPE-BIBLE.md` from top to bottom.
2. Check `## Current Step` and `## Last Completed`.
3. Resume at the next ⬜ Planned step.
4. **Do NOT recreate stripped components.** If a step says "delete `components/driver/tier-badge.tsx`", that file must stay deleted across all later steps.
